# Python SDK Implementation Coverage

## Analysis Details

| Field | Value |
| --- | --- |
| SDK repository | dnsid-py |
| SDK branch | `main` |
| SDK commit | `68ba790df83aabb370e28bee4ea24ac5f68afb41` (`main`) |
| SDK package version | `0.21.0` (unreleased commits since the release) |
| dnsid design docs commit | `615f18273251e04a0acdc89398e8aef4bbc113a9` (`main`) |
| Analysis date | 2026-09-24 |
| Validation commands | `uv run pytest -q` (1376 passed), `uv run ruff check dnsid tests`, `uv run mypy dnsid` (all clean); `.venv/bin/python harness/run.py --out /tmp/dnsid-2026-09-24-harness.md` (193/193), `.venv/bin/python harness/c2sp_matrix.py` (9 interoperable paths, 6 mutated bundles rejected). |

## Review scope — 2026-09-24 (`5591a96`..`68ba790`)

Both recorded baselines resolve and are ancestors; clean worktrees. Design commits `2a2399b`..`6e3b753` add configuration-loading design 12 and change 01/05 CLI-pointer, no-derivation, constructor-default and secret rules; fixtures and conformance unchanged. SDK commits: `08ab24f` requires trusted WBA audience; `9c5cb8e` pins identity and migration destination when entity-signing prepared events; `68ba790` moves environment/CLI parsing to design 12 `config_loading`, adds deployment JSON, merge, construction and convenience paths, and changes config dataclass defaults. Reviewed impacted constructor, profile, transport, registry, log-signing and public-surface rows; unaffected wire, JOSE/OIDC verifier and C2SP reader behavior retains its prior evidence. Native and shared suites passed at exact heads; previously open partials remain.

## Review scope — 2026-09-23 (`686d7bf`..`5591a96`, release `0.21.0`)

The reviewed branch tip `686d7bf` was squash-merged to `main` as `01251e0` (#24), so it is not a literal ancestor; `git diff 686d7bf 01251e0` is empty (tree-identical). Design delta (`5cc35bc`..`ccba7ea`): tracker files only; no `docs/sdk-design/` requirement or `fixtures/` change, no uncommitted changes. SDK delta: `5591a96` only bumps `pyproject.toml` to `0.21.0` and writes the `0.21.0` changelog entry. No source, test, dependency, or generated artifact changed, so every coverage row, citation, and open Partial finding is preserved; suites were rerun at the release head.

## Review scope — 2026-09-22 (second pass, `4e8c93a`..`686d7bf`)

Design delta (`c20013d`..`5cc35bc`): 01 `HTTPSFetcher Interface` now names `TransportConfig.privateAddressHosts` as the only exemption to reject-by-default and forbids built-in TLD/hostname exemptions; the injected-transport conflict rule and table add `privateAddressHosts` and malformed-entry rejection. 05 adds the `privateAddressHosts` field, the `DNSID_PRIVATE_HOSTS` loader variable, and a `Private Address Hosts` section (exact or leading-dot suffix, label-bounded, loopback/RFC 1918/RFC 4193 only, mixed and IP-literal rejected, per-hop), permits a loopback-HTTP `registryUrl` default, and rewrites `AgentRegistrationInput` so `environment` defaults to `production` and mode is determined only by `managed`/`zoneId`. 11 adds `transport?: TransportConfig` to the factory options.

SDK delta (3 commits): `cd7a8ad` deletes the implicit `.test` rule from `_matches_private_allowlist`, adds `is_private_or_loopback_ip` so a matching host may resolve only to loopback/private-use with mixed answers rejected, never exempts IP literals, adds entry validation and the injected-fetcher conflict for `private_address_hosts` to `_validate_transport_config`, stops the C2SP fetcher from stripping `private_address_hosts`, and adds `DNSID_PRIVATE_HOSTS` to `config_from_environment`; `c434ad9` moves entry validation into `safe_transport.make_ssrf_safe_transport` (so the C2SP factory, OIDC `transport_config`, and raw transports also reject malformed entries), IDNA-encodes entries before validation, and stops `_default_resolver` from self-allowlisting an IP-literal DoH host; `686d7bf` adds `_validate_identity_config` (every `IdentityConfig` field must be `str`; `publish_profile` must be a supported publish profile, `max_key_age` a permitted `ka` value, and `status_url`/`ek_url`/`ku_url`/`capabilities_url` HTTPS URIs without credentials or fragments) and type checks for `dns_server`, `ca_bundle_path`, and `private_address_hosts` in `_validate_transport_config`, all raising `ArgumentError` before any transport is built. Rows re-reviewed: construction validation, injected-transport conflicts, environment loader, `HTTPSFetcher`/SSRF guard, transport responsibilities, registry client/endpoints (design change only), C2SP factory. `dnsid/registry_client.py` is unchanged since `4e8c93a`.

## Review scope — 2026-09-22

History was rewritten when the repositories moved from the `Identity-Digital` organization to `dnsid-ai` for public release, so the recorded baselines (`1257fd0` SDK, `1d3fbb5` design) no longer resolve in the current clones. The missing history was obtained from the pre-migration clones: the old `1257fd0` tree differs from the new root commit `c9ae777` ("Initial public release", `0.19.1`) only by organization/URL renames and a `.gitleaksignore`, and the old design `1d3fbb5` differs from the new design root only by tracker/readiness files. The review therefore proceeded incrementally from `c9ae777` (SDK) and `1d3fbb5`-equivalent (design).

Design delta (`1d3fbb5`..`c20013d`, scoped to `docs/sdk-design/` and `fixtures/`): wording only — the `Identity Digital-Managed Trust` heading/anchor became `DNSid-Managed Trust` (already used by this tracker), the C2SP method-spec link became a revision reference, and `fixtures/parse-*.yaml`/`validate.yaml` moved lookalike/unrelated domains to RFC 2606 names with unchanged expectations. No behavior requirement changed; no uncommitted design changes.

SDK delta (`c9ae777`..`4e8c93a`, 22 commits): `25cbc83` adds `run_concurrently`/`BudgetExhausted` and runs the post-`sg` identity branch and the status fetch concurrently with fixed error precedence; `50c3995` additionally preloads the C2SP lifecycle history concurrently with the `ku` fetch; `42c1866`/`f930591` change `register_agent` defaults (production default, `managed` requires `zone_id`, sandbox accepted), make `DEFAULT_REGISTRY_URL` the loopback `dnsid local` registry, skip the credential requirement on loopback, and add `registry_client_from_environment`/`DNSID_API_KEY`; `aa46330` adds an implicit `.test`-TLD private-address exemption to the SSRF allowlist matcher and a `transport_config` option to the C2SP verification factory; the remainder are CI/release/provenance workflows, docs, examples, release-readiness files, and releases `0.19.2`–`0.20.0`. Impacted rows (`VerifyDomain` flow, status fetch, verification budget, `HTTPSFetcher`/SSRF, environment loader, registry client/endpoints, C2SP factory, `VerifiedDomain` construction) were re-reviewed and every citation in a changed file was refreshed; rows whose design text and cited files were both untouched are preserved. Previously recorded gaps (prepared-event `eventId`, OIDC buffered token response, conformance-metadata declarations, issuance-recovery evidence, constructor-time type validation of `identity`/`transport` string fields) are unchanged by this delta and remain open.

## Review scope — 2026-09-15

Follow-up (same day): reviewed `8490ca4`..`1257fd0` (three commits: `96b14a2` config-object unknown/missing fields raise `ArgumentError`, `VerifiedDomain.signing_key_thumbprint` retained and used by acceptance, `private_address_hosts` gains leading-dot suffix entries, profile docstrings on inherited transport; NOTICE copyright line; `0.19.1` release). Re-reviewed the configuration-validation, pin, `VerifiedDomain`, SSRF/transport, and error rows; line references refreshed in `dnsid/models.py`, `dnsid/safe_transport.py`, `dnsid/oidc.py`, and tests. Suites rerun at `1257fd0`.

Initial review: SDK commits `2fc0e9e`..`8490ca4` (seven commits: durable SQLite checkpoint store with typed consistency errors, atomic key-store persistence under a transaction lock, a dependency bump, thumbprint-derived kids plus registry `id`/`JWK.from_dict`/`cancel_agent`/typed registry errors, the `DnsidConfig` consolidation with counterparty acceptance (`f5b3080`), peer-certificate extraction from httpx's network stream, and the `0.19.0` release) from the recorded `53d4d60` baseline, and design commits `73d5aa1`, `dac6575`, `59a2696`, `7f9d681`, `664e281`, and `e773433` from `dd5ef3b`. Both baselines resolve and are ancestors of the reviewed heads; no root `fixtures/` or `conformance/` files changed, and there were no uncommitted design or SDK changes. The consolidated configuration/acceptance contract is a public API redesign, so every `Pre-redesign` and `TBD` row was re-reviewed against the settled design; rows whose cited files changed had line references refreshed, and rows whose design requirements and implementation files were both untouched are preserved. The former `Pre-redesign` marker is retired. Previously recorded gaps (prepared-event `eventId`, OIDC buffered token response, conformance-metadata declarations, issuance-recovery evidence) are unchanged by this delta and remain open.

## Coverage Checklist

### Document Set and Shared Contracts

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Document set and profile boundaries | [README: Document Set](../README.md#document-set) | Implemented: core `dnsid/manager.py:127`; draft 01 publication `dnsid/manager.py:460`, `:512`; profiles `dnsid/jose.py:64`, `dnsid/http_signatures.py:79`, `dnsid/oidc.py:199`; WBA signer/directory `dnsid/wba_signer.py:107`, `:306`; transport `dnsid/registry_client.py:54`; shared logs `dnsid/registry.py:16`; C2SP binding `dnsid/c2sp_tlog/reader.py:248`. | Numbered draft publication, pre-RFC `DNSid1` verification, local entity-key management, and WBA directory serving are present. |
| Shared profile contracts: identity verification, stable IDs, logs, errors, defaults | [README: Shared Profile Contracts](../README.md#shared-profile-contracts) | Implemented: `IdentityResolver` and peer certificate `dnsid/interfaces.py:41`; `{domain}#{kid}` parsing `dnsid/_utils.py:169`; split `ek`/`ku` verification and mandatory lifecycle checks `dnsid/manager.py:1499`, `:1586`, `:1596`; defaults/selectors `dnsid/models.py:56`, `:600`, `:613`; operation-level evidence `dnsid/models.py:1110`, `:1195`, `dnsid/manager.py:1669`; status freshness/fail-closed behavior `dnsid/manager.py:1307`, `:1746`. | Successful verification is fail-closed, profile verifiers propagate current-peer evidence, and operation-level log checks retain structured evidence. |
| Dependency direction between core, profiles, transport, registry, and logs | [README: Dependency Direction](../README.md#dependency-direction) | Implemented: profiles depend on `IdentityResolver`/`KeyProvider` (`dnsid/http_signatures.py:46`, `dnsid/jose.py:48`, `dnsid/oidc.py:27`); registry client is concrete transport (`dnsid/registry_client.py:54`); C2SP depends on shared interfaces/types (`dnsid/c2sp_tlog/reader.py:15`). |  |
| SDK naming conventions | [README: Naming](../README.md#naming) | Implemented: snake_case public methods including `verify_domain` `dnsid/manager.py:1231`, `create_txt_record` `:460`, and `get_key_set` `:428`. |  |

### SDK Configuration and Counterparty Acceptance

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| `DnsidConfig` single entry point: `identity?`, `verification`, `transport` sections; data-only config, runtime objects in deps | [01: Initialization](../01-core-identity-manager.md#initialization) | Implemented: `DnsidConfig`/`IdentityConfig`/`TrustedEntity`/`VerificationConfig`/`TransportConfig` at `dnsid/models.py:188`, `:56`, `:104`, `:124`, `:148`; ordinary constructor at `dnsid/manager.py:136` calls `validate_dnsid_config` (`:167`, `:1893`); runtime objects in `IdentityManagerDependencies` (`:91`, `:124`). | Non-`DnsidConfig` roots reject with `ArgumentError`; partial identity dataclasses are allowed as loader inputs but the constructor requires all identity fields. |
| Verification-only manager by omitting `identity`; key providers rejected with `ArgumentError`; idiomatic alias optional | [01: Initialization](../01-core-identity-manager.md#initialization) | Implemented: `identity is None` with `key_provider` or `deps.entity_key_provider` raises `ArgumentError` at `dnsid/manager.py:174`; `identity` without `key_provider` raises at `:180`; `for_verification(deps, verification=, transport=)` delegates to the constructor at `dnsid/manager.py:230`; local-only operations guard through `_local_identity` at `:262`. | Tests `tests/test_manager.py:130`, `:144`, `:151`, `:155`, `:162`, `:169`. |
| `IdentityConfig` contains only publication settings | [01: IdentityConfig](../01-core-identity-manager.md#identityconfig) | Implemented: `dnsid/models.py:56` carries only publication fields with absent scalar defaults for loading; `status_check_interval`/`dnssec_mode` are under verification. | Unknown keyword fields are rejected by the dataclass constructor (`tests/test_counterparty_acceptance.py:174`). |
| `VerificationConfig`: `statusCheckInterval`, `dnssecMode`, `trustedEntities?` with defaults | [01: VerificationConfig](../01-core-identity-manager.md#verificationconfig) | Implemented: `dnsid/models.py:124` represents absent DNSSEC as `None`, zero interval and absent allowlist; `_validate_verification_config` resolves absent mode to AUTO and rejects negative/non-duration intervals or invalid modes (`dnsid/manager.py:1914`); both manager modes use the same section (`:1432`, `:1356`). | Test `tests/test_counterparty_acceptance.py:113`. |
| Construction validates and snapshots config before network work; unknown fields, invalid values, duplicate normalized entities, invalid pins rejected | [01: Configuration Ownership and Loading](../01-core-identity-manager.md#configuration-ownership-and-loading) | Implemented: the constructor deep-copies the config (`dnsid/manager.py:164`) and runs `validate_dnsid_config` before building any transport (`:167`, `:1893`); `_validate_verification_config` checks and snapshots every verification field, rejecting invalid/duplicate normalized entities and invalid pins (`dnsid/manager.py:1914`); `_validate_transport_config` rejects mistyped/conflicting fields and malformed hosts (`:1999`, `dnsid/safe_transport.py:139`); the `config` property returns an independent copy (`dnsid/manager.py:243`). Dataclass keyword errors become `ArgumentError` (`dnsid/models.py:39`); `_validate_identity_config` (`dnsid/manager.py:1966`, called from `validate_dnsid_config` at `:1893`) requires domain/gi/log/status, rejects invalid publication URLs/profile/`ka`, and runs before transport/key/policy I/O. | Python dataclasses can carry mistyped values, so the design requires constructor-time rejection; all such checks now precede network work (resolver mock never called). Tests `tests/test_counterparty_acceptance.py:126`, `:165`, `:174`, `:235`, `:261`, `:271`; `tests/test_manager.py:496` (unsupported profile rejected at construction rather than first publish). Empty optional URL fields are still permitted at construction and enforced when the record is built. |
| Injected-transport conflicts: `dnsServer` configures whichever default consumer is not injected, rejected when both are; `caBundlePath` rejected when HTTPS fetcher is injected | [01: Configuration Ownership and Loading](../01-core-identity-manager.md#configuration-ownership-and-loading) | Implemented: `_validate_transport_config` rejects `dns_server` only when both `dns_resolver` and `https_fetcher` are injected, and `ca_bundle_path` or a non-empty `private_address_hosts` when `https_fetcher` is injected, at `dnsid/manager.py:1970`, `:1991`; malformed entries fail construction (`:1990`); defaults are built only for non-injected consumers at `dnsid/manager.py:200`, `:203`. Injected objects are not inspected. | Tests `tests/test_counterparty_acceptance.py:186`, `:196`, `:204`, `:285`, `:298`, `:305`. |
| Explicit loaders only; precedence rule (caller wins, omission ≠ empty, arrays replace); no inference of trust from local identity data | [01: Configuration Ownership and Loading](../01-core-identity-manager.md#configuration-ownership-and-loading), [12: Merge](../12-configuration-loading.md#merge) | Implemented: constructors read no sources (`dnsid/manager.py:136`); `load_environment`, `load_file`, `load_cli_directory` (`dnsid/config_loading.py:117`, `:183`, `:313`) return partials; `merge_loaded_config` (`:371`) merges fields and atomically replaces trust; `construct_identity_manager` (`:410`) validates before dependency I/O. | Documented dataclass default/zero limitations at `dnsid/config_loading.py:13`; no loader infers counterparties or supplies `noop:0`. Tests `tests/test_config_loading.py:644`, `:693`. |
| Deployment file: JSON `{dnsid, logTrust, registry}` | [12: Deployment File](../12-configuration-loading.md#deployment-file) | Implemented: `load_file` (`dnsid/config_loading.py:183`) rejects unknown/mistyped/duplicate fields (`:226`, `:555`, `:577`), maps three sections, excludes keys/credentials; `construct_identity_manager` rejects invalid log-trust selections (`:445`). | Tests `tests/test_config_loading.py:550`, `:632`. |
| CLI loader maps flat snake_case fields into `config.identity`; verification settings never inferred from publication data | [01: Initialization from DNSid CLI Configuration](../01-core-identity-manager.md#initialization-from-dnsid-cli-configuration), [12: Sources](../12-configuration-loading.md#sources) | Implemented: `load_cli_directory` (`dnsid/config_loading.py:313`) follows the root domain pointer (`:342`), maps only persisted publication fields (`:353`), resolves `entity_key_path` against selected config (`:358`); `identity_manager_from_dnsid` merges the overlay before construction (`:510`). | No status derivation or placeholder log ref; default location `~/.dnsid`, not `DNSID_CONFIG_DIR`. Tests `tests/test_config_loading.py:500`, `:859`. |
| `trustedEntities` exact `governanceId` match via `GovernanceID(record)`; no wildcard/suffix/transitive matches; `[]` denies all; absent makes no decision | [01: Counterparty Acceptance](../01-core-identity-manager.md#counterparty-acceptance), [07: Identity Record Behavior Profiles](../07-protocol-data-types.md#identity-record-behavior-profiles) | Implemented: `_enforce_counterparty_acceptance` at `dnsid/manager.py:1316` returns without a decision for `None`, compares each normalized configured `governance_id` exactly against the signed `record.gi`, and denies otherwise; an empty tuple denies all. | Tests `tests/test_counterparty_acceptance.py:321`, `:326`, `:332` (unrelated delegated agent domain), `:347`. |
| `entityKeyThumbprints` pins: canonical RFC 7638 SHA-256 thumbprints of the current record-signing key; no automatic pin learning or rotation expansion | [01: Counterparty Acceptance](../01-core-identity-manager.md#counterparty-acceptance), [05: Registry Custody and Pins](../05-transport-and-registry.md#registry-custody-and-pins) | Implemented: pins are compared against `result.signing_key_thumbprint` (the verified `ek` key) at `dnsid/manager.py:1322`, `:1326`; the thumbprint is computed once and retained on `VerifiedDomain` as a cached property (`dnsid/models.py:1182`); the operational key is never consulted; no code path adds pins. | Tests `tests/test_counterparty_acceptance.py:354` (asserts no recomputation), `:369`. |
| Acceptance ordering in `VerifyDomain`: after all protocol and current-peer checks, before final freshness check, on fresh lookups, status refreshes, and cache hits | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: the shared boundary `_verify` at `dnsid/manager.py:1299` runs `_reusable_verification` (fresh, coalesced, cache hit, or refresh), then `_enforce_mtls_policy` (`:1310`), then acceptance (`:1312`), then `_require_fresh_evidence` (`:1313`), for every invocation. | Tests `tests/test_counterparty_acceptance.py:411`, `:424`, `:438`. |
| Evidence cached before acceptance; denial and decision never cached; acceptance policy part of the verification context | [01: IdentityCache](../01-core-identity-manager.md#identitycache) | Implemented: fresh and refreshed evidence is `put` inside the reusable path at `dnsid/manager.py:1553`, `:1427` before acceptance runs; the acceptance check only raises (`:1316`); every manager wraps a supplied backend in a private namespace at `dnsid/manager.py:210`, `:2274`. | Tests `tests/test_counterparty_acceptance.py:411`, `:424`, `:464`. |
| `CounterpartyNotAccepted` permanent code with `VerifiedGovernanceID` and `VerifiedEntityKeyThumbprint`; no disclosure of configured allowlist or pins | [01: VerificationError](../01-core-identity-manager.md#verificationerror) | Implemented: `VerificationCode.COUNTERPARTY_NOT_ACCEPTED` at `dnsid/enums.py:77`; `verified_governance_id`/`verified_entity_key_thumbprint` on `VerificationError` at `dnsid/exceptions.py:97`, `:122`; the denial is raised non-transient with only observed values at `dnsid/manager.py:1333`. | Test `tests/test_counterparty_acceptance.py:380`. |
| `VerifiedDomain` carries no trust flag or cached acceptance decision | [07: VerifiedDomain](../07-protocol-data-types.md#verifieddomain) | Implemented: `dnsid/models.py:1138` holds protocol evidence only; `signing_key_thumbprint` (`:1182`) is derived evidence, not a decision. | Test `tests/test_counterparty_acceptance.py:396`. |
| Registry-managed publication confirmation uses the protocol-only internal path; public `VerifyDomain(localDomain)` still enforces acceptance | [05: AwaitRegistryManagedPublication](../05-transport-and-registry.md#awaitregistrymanagedpublicationregistryclient-registryclient---publishedrecord) | Implemented: `await_registry_managed_publication` calls the private `_verify_publication_evidence` at `dnsid/manager.py:665`, which passes `acceptance=False` (`:1295`); the only public entry point `verify_domain` passes `acceptance=True` (`:1231`, `:1293`). | Test `tests/test_counterparty_acceptance.py:486`. |
| Profile verifiers (JOSE, HTTP signatures, OIDC subject, WBA) inherit acceptance through the resolver; issuer-only OIDC and generic WBA make no acceptance claim and are never a fallback | [03: Profile Configuration](../03-jose-jwt-jws.md#profile-configuration), [09: VerifyOIDCToken](../09-oidc-federation.md#verifyoidctokentoken-string-opts-verifyoidctokenoptions---verifiedoidcsubject), [10: Configuration](../10-web-bot-auth.md#configuration) | Implemented: JOSE, HTTP signature, OIDC subject, and the JWT bot-token profile resolve identities through `IdentityResolver.verify_domain` (`dnsid/jose.py:267`, `:378`, `dnsid/http_signatures.py:551`, `dnsid/oidc.py:660`, `dnsid/web_bot_auth.py:545`), inheriting acceptance per invocation; local-identity fields come from `manager.local_domain` and profile settings stay profile-owned. OIDC `verify_dnsid_subject=False` is explicit and yields no verified domain (`dnsid/oidc.py:172`, `:660`). | Python ships no RFC 9421 WBA verifier (see Web Bot Auth), so the generic-mode rule is not applicable. Its JWT bot-token verifier now requires a trusted expected audience (`dnsid/web_bot_auth.py:370`). |
| Required validation matrix for configuration and acceptance scenarios | [01: Counterparty Acceptance](../01-core-identity-manager.md#counterparty-acceptance) | Implemented: `tests/test_counterparty_acceptance.py:113` (same defaults), `:126` (snapshot), `:165`/`:174` (invalid/unknown before network), `:186`–`:204` (transport matrix), `:321`/`:326` (omitted vs empty), `:332` (exact `gi`, unrelated agent), `:347` (child/suffix/lookalike), `:354`/`:369` (pins vs `ku`/other), `:380` (non-disclosure), `:396` (no trust flag), `:411`/`:424` (fresh denial and refresh keep evidence cached), `:438` (concurrent), `:464` (shared backend isolation), `:486` (publication vs public local verify); expiry-before-return `tests/test_corrected_boundaries.py:121`. | The deployment-file row is Not applicable for Python. |

### Configuration Loading (12)

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Partial schema and credentials | [12: Loaded Configuration](../12-configuration-loading.md#loaded-configuration), [12: Sources](../12-configuration-loading.md#sources) | Implemented: `LoadedConfig`, `LogTrust`, `KeySource` at `dnsid/config_loading.py:54`, `:73`, `:89`; registry API key is read only in `registry_client_from_environment` (`:538`). | Dataclass-default presence exceptions are documented at `:13`; secret is never stored in loaded config. |
| Environment schema and CLI directory | [12: Environment Variables](../12-configuration-loading.md#environment-variables) | Implemented: `load_environment` (`dnsid/config_loading.py:117`) trims only known fields, rejects invalid DNSSEC, reads bytes/profile and retains optional key paths; `load_cli_directory` (`:313`) follows authoritative root pointer and does not consult environment. | Tests `tests/test_config_loading.py:448`, `:859`. |
| Deployment JSON and validation | [12: Deployment File](../12-configuration-loading.md#deployment-file) | Implemented: `load_file` (`dnsid/config_loading.py:183`) parses three sections, rejects unknown, duplicate or mistyped members (`:226`, `:555`), omits `keySource` and credentials. | Trust-selection errors fail in construction (`:445`); tests `tests/test_config_loading.py:632`. |
| Field-wise merge and atomic log trust | [12: Merge](../12-configuration-loading.md#merge) | Implemented: `merge_loaded_config` and `_merge_fields` (`dnsid/config_loading.py:371`, `:387`) replace lists and whole `log_trust`; explicit empty `trusted_entities=[]` denies all (`tests/test_config_loading.py:225`). | Python dataclass defaults cannot clear loaded scalar defaults or an empty `private_address_hosts`; limitation documented at `dnsid/config_loading.py:13`, tested at `tests/test_config_loading.py:693`. |
| Construct: upfront validation, dependency and log trust precedence | [12: Construction](../12-configuration-loading.md#construction) | Implemented: `construct_identity_manager` validates before I/O, honors injected dependencies and selects CLI over store (`dnsid/config_loading.py:410`, `:475`); trust dispatcher enforces one variant, `managed:true`, 10-minute defaults and transport (`:445`). | Caller `deps.log_registry` bypasses loaded trust; tests `tests/test_config_loading.py:292`, `:302`, `:421`. |
| One-call managers and separate registry credential | [12: Convenience Constructors](../12-configuration-loading.md#convenience-constructors) | Implemented: environment, CLI and file manager conveniences (`dnsid/config_loading.py:494`, `:510`, `:522`) use Load → Merge → Construct; registry convenience (`:538`) lets the RegistryClient trim the secret (`dnsid/registry_client.py:130`). | Tests `tests/test_config_loading.py:373`, `:820`. |

### Core Identity Manager

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| `IdentityManager` type / facade | [01: IdentityManager](../01-core-identity-manager.md#identitymanager) | Implemented: `dnsid/manager.py:127`; local constructor `:136`; discoverable keyless `for_verification` `:230`; local mutation/signing guards `:414`, `:422`. | Verification-only construction requires no caller-supplied dummy identity or key provider. |
| Initialization config validation and dependency wiring | [01: Initialization](../01-core-identity-manager.md#initialization) | Implemented: config validation/snapshot and dependency wiring `dnsid/manager.py:136`, `:170`, `:184`; dependency type `:91`; entity provider `:124`, `:196`; local log binding derivation `:217`. | Separate operational and entity providers are wired; see the configuration/acceptance section for the full contract. |
| Configuration / options surface | [01: Initialization](../01-core-identity-manager.md#initialization) | Implemented: `IdentityConfig` with `ek_url`/`ku_url`/`publish_profile` `dnsid/models.py:56`, `:91`; `VerificationConfig`/`TrustedEntity` `:120`, `:100`; `TransportConfig` with hostname-scoped private-address policy (exact or leading-dot suffix entries) `:143`, `:178`; `DnsidConfig` `:183`; `RegistryConfig` `:205`; public snapshot accessor `dnsid/manager.py:250`. | Configuration is validated and snapshotted at construction; registry settings are owned by the registry client. |
| Environment-based configuration loader | [12: Environment Variables](../12-configuration-loading.md#environment-variables) | Implemented: `load_environment` (`dnsid/config_loading.py:117`) trims values, maps optional identity fields (`:139`), validates DNSSEC (`:141`), splits hosts (`:150`), reads trust files (`:152`), returns optional registry/key paths without API key (`:164`); one-call manager and registry constructors at `:494`, `:538`. | No default/derived URL or log ref; unknown tooling variables ignored. Tests `tests/test_config_loading.py:160`, `:448`, `:820`, `:852`. |
| CLI configuration loader | [01: Initialization from DNSid CLI Configuration](../01-core-identity-manager.md#initialization-from-dnsid-cli-configuration) | Implemented: `dnsid/config_loading.py:313` selects default directory, follows root pointer and maps persisted values (`:342`, `:353`); entity path and effective-domain key loading (`:358`, `:475`); JWK/PEM support `dnsid/local_key_provider.py:409`, `:462`. | `DNSID_CONFIG_DIR` is environment `key_source` only; `identity_manager_from_dnsid` reads `~/.dnsid` unless explicitly given a directory. |
| Local log signing/write split (`CanonicalizeLogEvent`, `RequiredLogSignatures`, `SignEvent`, `WriteSignedEvent`, `SignAndWriteEvent`) | [01: Core Methods](../01-core-identity-manager.md#core-methods) | Implemented: `dnsid/manager.py:273`, `:296`, `:319`, `:352`, `:377`; provider-explicit primitive `:1818`; role table `:1862`. | Entity and operational roles route to separate providers; canonicalization excludes signature fields. |
| `GetKeySet()` | [01: GetKeySet](../01-core-identity-manager.md#getkeyset---jwks) | Implemented: `dnsid/manager.py:428`. | Returns exactly the current operational key. |
| `GetEntityKeySet()` | [01: GetEntityKeySet](../01-core-identity-manager.md#getentitykeyset---jwks) | Implemented: `dnsid/manager.py:442`. | Returns exactly the current entity key and fails when no entity provider is configured. |
| `CreateTxtRecord()` | [01: CreateTxtRecord](../01-core-identity-manager.md#createtxtrecord---string) | Implemented: `dnsid/manager.py:460`; unsigned builder/default selector `:2217`; key distinctness `:494`, `:505`. | Emits exact `v=dnsid-draft-01`, uses the entity provider, and rejects pre-RFC `DNSid1` publication. |
| `VerifyDomain(domain, peerCert?)` end-to-end flow | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: `dnsid/manager.py:1231`; parse/validate `:1478`, `:1489`; `ek` fetch and `sg` verification `:1499`; post-`sg` fan-out `:1529` (`_verify_identity_evidence` `:1556`, `_fetch_active_status` `:1631`); `ku` fetch with concurrent history preload `:1581`-`:1585`; lifecycle checks `:1603`; per-caller mTLS `:1310`; status `:1634`; result evidence `:1534`. | Authenticates `sg` before following `su`, `ku`, or `lr`. Both selectors preserve exact `v=` bytes and require bilateral binding plus continuity. Since `25cbc83`/`50c3995` the identity branch and the status fetch run concurrently under the shared budget via `run_concurrently` (`dnsid/_verification_budget.py:102`), and C2SP readers warm their complete history (`dnsid/c2sp_tlog/reader.py:451`) alongside the `ku` fetch; the first definitive failure cancels its sibling and precedence is identity error > status error > cancellation (`tests/test_post_sg_fanout.py:32`, `:69`, `:94`, `:131`, `:173`, `:191`). A cancelled sibling blocked in a network call is abandoned to its own bounded timeout because Python cannot interrupt it. |
| DNS TXT lookup at `_dnsid.{domain}` | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: `dnsid/manager.py:1437`. |  |
| TXT record parsing and exactly-one-record validation | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: cardinality `dnsid/manager.py:1462`; parser `dnsid/models.py:679`; exact selector allow-list `:613`; unsupported-selector rejection `:695`. | Unknown future selectors fail closed without rewriting the wire value. |
| JWKS fetch and bounded response handling | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: split fetches `dnsid/manager.py:1499`, `:1586`; initial/redirect URL checks and streamed exact-200 1 MiB handling `dnsid/_https_client.py:33`, `:49`, `:58`, `:67`, `:73`; checked-IP transport `dnsid/safe_transport.py:76`, `:149`; signed-record URL validation `dnsid/models.py:819`, `:2160`. | The default helper rejects credentials and fragments before fetching, validates each redirect, and enforces its decoded cap while streaming. |
| Status fetch and active status validation | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: full and refreshed status checks are at `dnsid/manager.py:1631`, `:1416`; transient `StatusUnavailable` remapping and semantic-error wrapping are at `:1746`, `:1755`; streamed parsing is at `dnsid/_https_client.py:119`, `:205`; strict type/state validation is at `dnsid/models.py:574`. | Transport failure is indeterminate and never returns stale success; malformed documents become non-transient `VerificationError(RecordInvalid)` with a chained `ValidationError`, and valid non-ACTIVE status is rejected. |
| `AgentStatus` validation and workflow-state separation | [07: AgentStatus](../07-protocol-data-types.md#agentstatus), [06: Implemented lifecycle interpretation](../06-log-bindings.md#implemented-lifecycle-interpretation) | Implemented: model/validation `dnsid/models.py:555`, `:574`; authoritative nested protocol-status parsing `dnsid/_https_client.py:205`; ACTIVE-only verification `dnsid/manager.py:1636`; tests `tests/test_https_client.py:26`, `:39`, `:69`. | Accepts only the six exact protocol states and does not reinterpret registry workflow states. |
| Signature validation against JWKS keys | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: single `ek` key and canonical bytes `dnsid/manager.py:1501`, `:1505`; bare-signature parse/verify `:1506`, `:1512`, `:1518`; operational key and distinctness `:1586`, `:1590`. | Draft 01 requires explicit consistent JWK `alg`, one current key per role, and bare unpadded base64url `sg`. |
| Policy flag enforcement, including mTLS behavior | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: per-caller enforcement after reusable verification `dnsid/manager.py:1309`, `:1310`; RFC 9525-style SAN matcher `dnsid/models.py:515`; profile propagation `dnsid/jose.py:267`, `dnsid/http_signatures.py:551`, `dnsid/oidc.py:655`. | `fl=mtls` checks the current invocation's peer certificate before every successful return, including fresh and stale cache hits. |
| Operation-level log evidence (`RequiresLogCheck`, `VerifyLogEvidence`) | [01: VerifyLogEvidence](../01-core-identity-manager.md#verifylogevidencevd-verifieddomain-at-timestamp---loggedstateevidence) | Implemented: evidence model `dnsid/models.py:1110`; policy accessor `:1195`; manager helper/default operation time `dnsid/manager.py:1669`; evidence-returning interface `dnsid/interfaces.py:236`; C2SP verification and evidence construction `dnsid/c2sp_tlog/reader.py:556`, `:585`. | The helper remains operation-level, does not refresh protocol status, and returns the accepted stitched history bounds, completeness mode, checkpoint, and freshness time. |
| Cache read/write and freshness behavior | [01: IdentityCache](../01-core-identity-manager.md#identitycache) | Implemented: cache hit, per-call mTLS, coalesced miss/status refresh, shared failure, stale-error rejection, and copy-before-update `dnsid/manager.py:1310`, `:1352`, `:1363`, `:1399`, `:1415`; thread-safe `_InMemoryCache` `:2300`; expiry including `ek` TLS `dnsid/models.py:1199`. | Verification failures are not cached; same-domain reusable work is coalesced while different domains proceed independently. Eligible evidence is cached before acceptance and denials are never cached (`tests/test_counterparty_acceptance.py:411`). |
| Domain-log loading and cache eviction | [01: LoadDomainLog](../01-core-identity-manager.md#loaddomainlogvd-verifieddomain---domainlog), [01: EvictDomain](../01-core-identity-manager.md#evictdomaindomain-string) | Implemented: eviction `dnsid/manager.py:1687`; loading `:1691`; lifecycle replay `dnsid/c2sp_tlog/reader.py:718`. |  |
| Error types and verification codes | [01: Error Types](../01-core-identity-manager.md#error-types) | Implemented: error classes `dnsid/exceptions.py:22`; codes `dnsid/enums.py:46`; `COUNTERPARTY_NOT_ACCEPTED` and observed-identity fields `dnsid/enums.py:77`, `dnsid/exceptions.py:97`; typed registry request errors `dnsid/exceptions.py:158`; lifecycle categories/errors `dnsid/enums.py:117`, `dnsid/exceptions.py:132`; live status wrapping `dnsid/manager.py:1746`, `:1755`. | Live failures preserve structured code/transient/state fields and chained causes; parse and validation failures encountered during verification are adapted to non-transient verification errors. `RegistryRequestError` keeps the `LOG_ERROR` category, exposes HTTP status and registry error code, and never echoes response bodies. |
| `KeyProvider` interface and local/cloud providers | [01: KeyProvider Interface](../01-core-identity-manager.md#keyprovider-interface) | Implemented: interface `dnsid/interfaces.py:80`; `sign_key` `:130`; `supersede` `:160`; local provider `dnsid/local_key_provider.py:313`, `:527`, `:605`; atomic write under an exclusive file lock `:139`, `:165`; AWS provider `dnsid/aws_kms_key_provider.py:259`, `:441`. | Local EdDSA/ES256 generation and rotation are supported; the key store is persisted atomically (temp file, fsync, rename) under a transaction lock and generated kids are RFC 7638 thumbprints (`tests/test_local_key_provider.py:132`, `tests/test_generated_kid_is_thumbprint.py`). |
| Key generation, activation, supersede, rotation flow | [01: Operational Key Rotation](../01-core-identity-manager.md#operational-key-rotation), [01: Managed Rotation Recovery Contract](../01-core-identity-manager.md#managed-rotation-recovery-contract) | Implemented: managed rotation `dnsid/manager.py:684`; durable signing/persist/pause ordering `:777`, `:783`, `:800`, `:811`; resume `:828`; accepted reconciliation `:1044`; persisted-state validation `:1111`; mandatory hooks `:1188`. | Exact prepared bytes and state are persisted before submission and through every recoverable transition. |
| `HTTPSFetcher` interface and secure HTTP defaults | [01: HTTPSFetcher Interface](../01-core-identity-manager.md#httpsfetcher-interface) | Implemented: interface `dnsid/interfaces.py:447`; streamed exact-200 JWKS/status helpers and URL checks `dnsid/_https_client.py:33`, `:49`, `:119`, `:130`, `:261`; checked-resolution transport and exact/suffix host allowlist `dnsid/safe_transport.py:76`, `:63`, `:285`, `:149`. Private-address opt-in: `TransportConfig.private_address_hosts` (`dnsid/models.py:178`) entries are validated by `is_private_address_host_entry`/`validate_private_address_hosts` (`dnsid/safe_transport.py:114`, `:139`: IDNA-encoded, bare hostname or leading-dot suffix; IP literals, ports, schemes, paths, credentials, empty rejected with `ArgumentError`) both at manager construction (`dnsid/manager.py:1990`) and inside `make_ssrf_safe_transport` (`dnsid/safe_transport.py:156`); `_matches_private_allowlist` (`:285`) matches exact or label-bounded suffix entries case-insensitively with no built-in exemption; `resolve_checked_address` (`:93`) never exempts IP literals and, for a matching host, requires every address to satisfy `is_private_or_loopback_ip` (`:48`) so link-local/reserved and mixed public+private answers are rejected. | TLS, finite timeout, redirect count, streaming decoded-size enforcement, exact HTTP 200, credential/fragment rejection, DNS-rebinding-safe routing, and hostname-scoped private-service access are present. Matches design 01/05: rejected by default, explicit hostname-scoped allowlist as the only exception, no built-in exemption. Tests `tests/test_ssrf_guard.py:128` (no implicit `.test`), `:134`, `:142` (suffix/exact), `:151`, `:156` (address classes, mixed), `:163` (literals), `:273`–`:303` (validation choke point, C2SP factory, IDN). |
| HTTPS timeout, redirect, CA bundle, body-size limits | [01: HTTPSFetcher Interface](../01-core-identity-manager.md#httpsfetcher-interface) | Implemented: finite constants `dnsid/_https_client.py:19`; redirect checks `:261`, `:284`; CA augmentation `dnsid/safe_transport.py:158`; streaming body checks `dnsid/_https_client.py:73`, `:159`, `:243`. | JWKS and status decoded-body limits are enforced during reads and share the invocation budget. |
| SSRF-safe HTTP transport / fetch guard | [01: HTTPSFetcher Interface](../01-core-identity-manager.md#httpsfetcher-interface) | Implemented: all-address validation, exact/leading-dot-suffix host allowlisting, and checked-IP connection `dnsid/safe_transport.py:76`, `:63`, `:285`, `:191`; configuration `dnsid/models.py:143`, `:178`; pooled core transport `dnsid/manager.py:203`, `:1717`, `:1744`; OIDC reuse `dnsid/oidc.py:833`, `:842`. | Public reads fail closed on any non-global result unless the normalized hostname matches an explicitly configured exact entry or `.suffix` entry; suffix matching is label-bounded (`evildnsid.test` does not match `.dnsid.test`; `tests/test_ssrf_guard.py:117`). The design requires a hostname-scoped opt-in allowlist; suffix entries are hostname-scoped and opt-in and there is no built-in rule. The C2SP fetcher now passes `transport_config` through unchanged (`dnsid/c2sp_tlog/resource_fetcher.py:76`), so log reads honour the same list; `_default_resolver` self-allowlists only a hostname DoH server, never an IP literal (`dnsid/_default_resolver.py:109`). |
| `DNSResolver` interface and default/system/custom/DoH behavior | [01: DNSResolver Interface](../01-core-identity-manager.md#dnsresolver-interface) | Implemented: interface `dnsid/interfaces.py:426`; default resolver `dnsid/_default_resolver.py:17`; standard/custom DNS `:50`; DoH `:98`; secure-channel AD handling `:162`; system resolver `:185`; manager default `dnsid/manager.py:1997`. | Only explicit HTTPS DoH may map AD to `VALID`; system, ordinary DNS, and plain HTTP report `UNKNOWN`. |
| DNS TTL capture and cache TTL use | [01: DNSResolver Interface](../01-core-identity-manager.md#dnsresolver-interface) | Implemented: `TXTRecord.ttl` `dnsid/models.py:933`; cache expiry `dnsid/models.py:1199`. |  |
| DNSSEC state and enforcement modes | [01: DNSSECState](../01-core-identity-manager.md#dnssecstate) | Implemented: enums `dnsid/enums.py:12`, `:29`; strict policy checks `dnsid/manager.py:1433`, `:1446`, `:1470`, `:1479`; tests `tests/test_verify_domain.py:431`, `:484`. | `AUTO` preserves `UNKNOWN`; `VALIDATED` rejects `UNKNOWN`; `REQUIRED` accepts only `VALID`; `FAILED` always aborts. |
| Initial setup local identity flow | [01: Initial Setup](../01-core-identity-manager.md#initial-setup-of-local-identity) | Partial: publication key sets and record `dnsid/manager.py:428`, `:442`, `:460`; ISSUANCE signer roles `:1862`; C2SP split preparation `dnsid/c2sp_tlog/prepared.py:119`. | Key/signing primitives are established; these citations do not substantiate durable initial-issuance intent, restart recovery, exact-byte retries, acceptance binding, or idempotent completion. Registry coordination alone does not prove SDK/caller recovery. This is an evidence gap, not proof that every supported composition lacks it. |
| Registry status change / revocation flow | [01: Registry Status Change](../01-core-identity-manager.md#registry-status-change-eg-revocation) | Implemented: owner-authorized typed revoke request with immutable agent ID `dnsid/registry_client.py:328`; reason namespace `dnsid/enums.py:105`. | The registry is the sole append/retry coordinator; obsolete client-prepared later-event APIs were removed. |
| Retry / backoff behavior for transient failures | [01: VerificationError](../01-core-identity-manager.md#verificationerror) | Implemented: sync/async retry helpers `dnsid/retry.py:13`, `:54`. |  |
| Core non-goals honored | [01: Non-Goals](../01-core-identity-manager.md#non-goals) | Implemented: core does not publish DNS/certificates/blockchain; registry control plane is separate (`dnsid/registry_client.py:54`). |  |

### Verification Boundaries

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Shared finite verification budget | [README: Verification Resource Budgets](../README.md#verification-resource-budgets) | Implemented: public context manager/decorator `dnsid/_verification_budget.py:66`, `:157`; independent waiter timeout `:92`; `BudgetExhausted` cancellation marker `:31`; `run_concurrently` copies the caller's context into a helper thread so the sibling inherits and cannot extend the deadline `:102`; socket/TLS remaining-time enforcement `dnsid/safe_transport.py:218`; core `dnsid/manager.py:1230`; profiles `dnsid/jose.py:188`, `dnsid/http_signatures.py:363`, `dnsid/oidc.py:562`; C2SP boundary `dnsid/c2sp_tlog/reader.py:224`. | Default 30 seconds, nested operations cannot extend it; cancellation is cooperative and injected blocking dependencies must honor remaining time. Injected `HTTPSFetcher` implementations must now be safe for concurrent use (`dnsid/interfaces.py:457`). |
| Cache isolation, acquisition-based expiry, and zero TTL | [01: IdentityCache](../01-core-identity-manager.md#identitycache) | Implemented: configuration snapshots/private backend `dnsid/manager.py:164`, `:210`; DNS acquisition `:1436`; return-time evidence checks `:1642`; cache wrappers `:2274`, `:2300`; absolute expiry `dnsid/models.py:1199`; regression `tests/test_corrected_boundaries.py:121`, `:163`. | Status refresh cannot revive expired evidence; zero-TTL results are not cached or reused by waiting callers. Trust or acceptance-policy changes require a new manager; the private per-manager namespace already isolates acceptance contexts (`tests/test_counterparty_acceptance.py:464`). |
| Strict JOSE/OIDC parsing, dates, presence defaults, and expected audience | [03: Parsing and validation boundaries](../03-jose-jwt-jws.md#parsing-and-validation-boundaries), [09: Profile Configuration](../09-oidc-federation.md#profile-configuration) | Implemented: bounded strict compact/JSON/header/date helpers `dnsid/jose.py:424`, `:458`, `:480`, `:491`; config/explicit expiry `:111`, `:149`; trusted audience `:218`; expiration recheck `:300`; OIDC reuse and recheck `dnsid/oidc.py:602`, `:666`, `:1003`; regression `tests/test_corrected_boundaries.py:21`. | Rejects duplicate JSON, unsupported critical/unencoded headers, mixed audiences, nonnumeric dates, and invalid explicit durations. Zero/fractional NumericDates are valid types; clock skew never extends expiry. |
| HTTP input bounds and current-peer propagation | [02: Resource limits](../02-rfc9421-http-message-signatures.md#resource-limits) | Implemented: `dnsid/http_signatures.py:406`, `:553`; regression `tests/test_corrected_boundaries.py:99`. | 64 KiB combined signature headers, 16 KiB digest header, 8 MiB buffered body, 16 labels, 64 components. Hosts must enforce body bounds while reading the already-buffered request. |

### HTTP Message Signatures

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Constrained component and Structured Field model | [02: Structured Field data model](../02-rfc9421-http-message-signatures.md#structured-field-data-model) | Implemented: ordered `SignatureParams` parsing/serialization `dnsid/models.py:1501`, `:1564`, `:1571`, `:1690`; component validation/base construction `dnsid/http_signatures.py:597`. | Unknown signature parameters are preserved; unsupported component parameters and duplicates fail closed. |
| Canonical signature-base construction | [02: Signature-base construction](../02-rfc9421-http-message-signatures.md#signature-base-construction), [shared vectors](../conformance/README.md#http-message-signatures) | Implemented: `dnsid/http_signatures.py:597`; shared HTTP-message-signature vectors pass. | Includes `@query-param`, dictionary-key selection, extension serialization, and duplicate rejection. |
| DNSid request signing | [02: DNSid request profile](../02-rfc9421-http-message-signatures.md#dnsid-request-profile) | Implemented: request/client helpers `dnsid/http_signatures.py:149`, `:233`, `:287`; content-presence handling `:257`, `:274`, `:317`, `:341`; manager transport inheritance `:88`, `:355`. | Exact profile defaults, nonce/algorithm policy, supplied empty-content semantics, and inherited transport settings match the design. |
| DNSid verification and Content-Digest | [02: Verification](../02-rfc9421-http-message-signatures.md#verification), [02: Content-Digest](../02-rfc9421-http-message-signatures.md#content-digest) | Implemented: candidate selection `dnsid/http_signatures.py:364`; candidate/digest verification `:459`, `:510`; current-peer propagation `:551`. | Accepts exactly one valid DNSid candidate and ignores unrelated invalid complete signatures. |

### JOSE JWT and JWS

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| JOSE standards/dependencies and profile configuration | [03: Standards](../03-jose-jwt-jws.md#standards) | Implemented: `JoseProfile` `dnsid/jose.py:64`; `JoseConfig` `:55`. | Manual compact JOSE uses SDK crypto and base64url helpers. |
| Application-layer algorithm policy | [03: Application-Layer Algorithm Policy](../03-jose-jwt-jws.md#application-layer-algorithm-policy) | Implemented: EdDSA/ES256 allow-list `dnsid/_utils.py:18`; enforced for JWT/JWS `dnsid/jose.py:282`, `:368`; both available from `LocalKeyProvider.generate` `dnsid/local_key_provider.py:527`. |  |
| `CreateJWT` / claims / reserved claim validation | [03: CreateJWT](../03-jose-jwt-jws.md#createjwtopts-jwtoptions---string) | Implemented: `dnsid/jose.py:128`; `JWTOptions` `dnsid/models.py:1449`; reserved claims `dnsid/jose.py:154`. |  |
| `VerifyJWT(jwt)` | [03: VerifyJWT](../03-jose-jwt-jws.md#verifyjwtjwt-string---verifieddomain) | Implemented: `dnsid/jose.py:189`; current-peer forwarding `:267`. | Depends on fail-closed `verify_domain`, inheriting configured acceptance; JOSE settings stay in `JoseConfig`. |
| `CreateJWS(payload)` / `VerifyJWS(jws)` | [03: CreateJWS](../03-jose-jwt-jws.md#createjwspayload-bytes---string) | Implemented: create `dnsid/jose.py:310`; verify `:344`; current-peer forwarding `:389`. |  |
| Application-layer key IDs and flows | [03: Application-Layer Key IDs](../03-jose-jwt-jws.md#application-layer-key-ids) | Implemented: `parse_key_id` `dnsid/_utils.py:169`; manager profile factory `dnsid/jose.py:73`. |  |

### Transport and Registry

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Transport responsibilities and secure defaults | [05: Transport Responsibilities](../05-transport-and-registry.md#transport-responsibilities) | Implemented: `TransportConfig` as `DnsidConfig.transport` `dnsid/models.py:147`; injected-consumer conflicts `dnsid/manager.py:1991`; checked-IP/CA transport and allowlisting `dnsid/safe_transport.py:63`, `:285`, `:149`; explicit environment loader `dnsid/config_loading.py:117`; streamed core HTTPS `dnsid/_https_client.py:33`, `:119`, `:261`; peer certificate read `:329`. | DNS and CA injection are implemented without global mutation; core response limits are streamed and private-service access is hostname-scoped. |
| `CreateDnsidHttpClient(transportConfig)` | [05: CreateDnsidHttpClient](../05-transport-and-registry.md#creatednsidhttpclienttransportconfig-transportconfig---httpclient) | Implemented: sync and async factories `dnsid/manager.py:1784`, `:1798`. |  |
| Registry client and default URL | [05: RegistryClient](../05-transport-and-registry.md#registryclient) | Implemented: base URL validation `dnsid/registry_client.py:87`, `:112`, `:115`; constructor default `dnsid/registry_client.py:106` uses `DEFAULT_REGISTRY_URL` (`dnsid/models.py:202`, `http://127.0.0.1:7755`); loopback credential handling at `dnsid/registry_client.py:173`, `:176`; concrete client `:54`. | HTTPS except exact literal loopback hosts; hosted callers explicitly set the URL. `registry_client_from_environment` (`dnsid/config_loading.py:538`) is the only environment reader; credentials stay out of loaded configuration. |
| Publication authority model (client vs registry) | [05: Registry Responsibilities](../05-transport-and-registry.md#registry-responsibilities) | Implemented: typed authority/configuration `dnsid/models.py:1776`, `:1790`; fail-closed protocol and registration response parsing `dnsid/registry_client.py:1474`, `:1502`; client and registry gates `dnsid/manager.py:512`, `:547`, `:608`, `:648`. | Neither path substitutes the operational key for the entity key. |
| Managed Live registration and proof workflow | [05: LiveAgentRegistrationInput](../05-transport-and-registry.md#liveagentregistrationinput), [05: Live Provisioning Types](../05-transport-and-registry.md#live-provisioning-types) | Implemented: dedicated models are at `dnsid/models.py:1766`, `:1821`, `:1835`, `:1845`, `:1855`, `:1867`; exact-status authenticated operations are at `dnsid/registry_client.py:273`, `:472`, `:503`; public Ed25519 validation and transcript/key/domain binding are at `:1249`, `:1291`, `:1386`. | Reissue requires the original public key as non-wire context, validates it before network I/O, computes its RFC 7638 thumbprint, and binds the replacement transcript to it. |
| Client-controlled registry publication | [05: PublishClientControlledRecord](../05-transport-and-registry.md#publishclientcontrolledrecordregistryclient-registryclient---publishedrecord) | Implemented: `dnsid/manager.py:512`; authority/key gates `:547`, `:562`; parse/profile/tag comparison `:570`, `:578`, `:592`; signature submission `:600`; `ka` mismatch tests `tests/test_manager.py:437`, `:448`, `:461`. | All known tags are compared exactly and unknown signed tags are preserved. |
| Registry-managed publication wait | [05: AwaitRegistryManagedPublication](../05-transport-and-registry.md#awaitregistrymanagedpublicationregistryclient-registryclient---publishedrecord) | Implemented: `dnsid/manager.py:608`; authority/status checks `:648`, `:658`, `:662`; protocol-only DNS re-verification without acceptance `:665`, `:1295`; terminal registry status classification `dnsid/registry_client.py:47`. | Registry READY/DNS workflow state is not trusted as protocol verification; `REVOKED` and `RETIRED` are terminal. Public `verify_domain(local_domain)` still enforces acceptance (`tests/test_counterparty_acceptance.py:486`). |
| Registry endpoints and ordinary lifecycle helpers | [05: RegistryClient](../05-transport-and-registry.md#registryclient), [05: AgentRegistrationInput](../05-transport-and-registry.md#agentregistrationinput) | Implemented: ordinary registration `dnsid/registry_client.py:187` defaults an omitted `environment` to `"production"` (`:202`), accepts `"sandbox"` as a non-managed environment, and requires `zone_id` for `managed=True` (`:208`); exact HTTP 201 handling `:242`; verify/revoke/retire/cancel/challenge `:309`, `:328`, `:366`, `:380`, `:394`; status/poll `:529`, `:665`; canonical/publish `:807`, `:827`; prepare/submit `:846`, `:897`, `:978`; non-2xx responses raise typed `RegistryRequestError` without echoing bodies `:1119`, `dnsid/exceptions.py:158`. | Design 05 `AgentRegistrationInput` (as of `5cc35bc`) documents exactly this contract: `environment` defaults to `production`, mode is determined only by `managed`/`zone_id`, `managed=True` requires `zone_id`. Prepared events remain limited to issuance and rotation. Revocation and retirement are registry-owned mutation calls. Tests `tests/test_registry_client.py:297`, `:304`, `:317`, `:323`, `:352`, `:360`. |
| Managed operational key rotation flow (`ku` supersede) | [05: RegistryClient](../05-transport-and-registry.md#registryclient) | Implemented: `dnsid/manager.py:684`, `:828`, `:960`, `:1044`, `:1088`; registry prepare/submit `dnsid/registry_client.py:897`, `:978`; durable result `dnsid/models.py:1982`. | New-key activation occurs only after an accepted result binds to the exact persisted bytes and key. |
| `KeyRotationPreparationRequest` data type | [05: KeyRotationPreparationRequest](../05-transport-and-registry.md#keyrotationpreparationrequest) | Implemented: `dnsid/models.py:1938`; private-member rejection and kid/alg validation `dnsid/registry_client.py:928`, `:933`. |  |
| `SubmissionResult` typed result | [05: RegistryClient](../05-transport-and-registry.md#registryclient) | Implemented: `dnsid/models.py:1957`; response normalization and exact accepted hash `dnsid/registry_client.py:978`, `:1102`, `:1072`. | Pending/rejected results retain stable error codes; accepted responses must bind to submitted bytes. |
| Registry data types | [05: Registry Responsibilities](../05-transport-and-registry.md#registry-responsibilities) | Implemented: ordinary/Live registration and proof models `dnsid/models.py:1750`, `:1766`, `:1790`, `:1821`, `:1835`, `:1845`, `:1855`, `:1867`; publication/prepared-event models `:1930`, `:1938`, `:1957`; strict parsing `dnsid/registry_client.py:1291`, `:1502`. | Workflow, DNS publication, and protocol status remain separate; `AgentRegistration.oidc_issuer_url` preserves the exact registry issuer at `dnsid/models.py:1801`. |

### Log Bindings

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Log responsibilities and interfaces | [06: Responsibilities](../06-log-bindings.md#responsibilities) | Implemented: `Log` `dnsid/interfaces.py:178`; `LogReader`, exact-cutoff contract, and evidence-returning non-revocation contract `:211`, `:263`, `:236`; exact-cutoff result `dnsid/models.py:1101`; bilateral/continuity hooks `dnsid/interfaces.py:292`, `:314`; C2SP implementation `dnsid/c2sp_tlog/reader.py:729`. | Lifecycle read/write responsibilities, method-neutral bounded predecessor verification, and structured operation-level evidence are implemented. |
| `DomainLog`, `DomainSnapshot`, shared lifecycle reducer | [06: Implemented lifecycle interpretation](../06-log-bindings.md#implemented-lifecycle-interpretation), [06: DomainLog](../06-log-bindings.md#domainlog) | Implemented: `DomainSnapshot`/`DomainLog` `dnsid/models.py:1243`, `:1260`; reducer `:1270`; C2SP terminal-history checks `dnsid/c2sp_tlog/reader.py:483`, `:548`, `:574`. | Verified history remains separate from current protocol status. |
| `LogRegistry` | [06: LogRegistry](../06-log-bindings.md#logregistry) | Implemented: `dnsid/registry.py:16`. |  |
| Production log reader / no-op behavior | [06: LogReader Interface](../06-log-bindings.md#logreader-interface) | Implemented: fail-closed `NoopLogReader` `dnsid/interfaces.py:342`; C2SP reader `dnsid/c2sp_tlog/reader.py:248`; draft 01 capability enforcement `dnsid/manager.py:2249`. |  |
| Required and optional log events | [06: Log Events](../06-log-bindings.md#log-events) | Implemented: event models `dnsid/models.py:971`, `:994`; C2SP mapping `dnsid/c2sp_tlog/event_codec.py:67`; signer roles `dnsid/c2sp_tlog/prepared.py:37`; lifecycle verification `dnsid/c2sp_tlog/stream_verifier.py:404`. | All six version-1 lifecycle event types and role signatures are implemented. |
| Method-owned metadata and verified lifecycle ordering | [06: Method-owned metadata](../06-log-bindings.md#method-owned-metadata) | Implemented: generic lifecycle-only events `dnsid/models.py:971`; C2SP context/chain metadata `dnsid/c2sp_tlog/event_codec.py:47`; index-order selection `dnsid/c2sp_tlog/stream_verifier.py:87`; prefix/state-machine enforcement `dnsid/models.py:1270`. | C2SP owns and verifies method metadata before returning ordinary lifecycle events. |

### C2SP Transparency Log Binding

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| C2SP binding responsibilities and package surface | [11: Responsibilities](../11-c2sp-tlog-binding.md#responsibilities) | Implemented: exports `dnsid/c2sp_tlog/__init__.py:191`; reader/binding `dnsid/c2sp_tlog/reader.py:248`, `:1392`. |  |
| Exact C2SP specification version identification | [11: Standards and Versioning](../11-c2sp-tlog-binding.md#standards-and-versioning) | Implemented: complete pinned `C2SP_SPEC_REVISIONS` `dnsid/c2sp_tlog/__init__.py:175`; envelope version `:188`; exact-map test `tests/test_public_api.py:63`. | The corrected DNSid method revision is additionally pinned in `dnsid/conformance.py:32`. |
| C2SP trust profiles and signer rotation | [11: Trust Profiles](../11-c2sp-tlog-binding.md#trust-profiles) | Implemented: exact-member parser `dnsid/c2sp_tlog/trust_profile.py:34`; scope/prefix, policy-origin, key-ID, key-distinctness, and role-separation validation `:76`, `:94`, `:98`; public exports `dnsid/c2sp_tlog/__init__.py:154`, `:191`. | Caller-supplied version-1 profiles constrain readers to one exact scope and canonical log prefix and support overlapping verifier keys. |
| `c2sp-tlog` reference parsing, identity-instance generation, canonicalization, origin, and final index | [11: Reference and Binding Context](../11-c2sp-tlog-binding.md#reference-and-binding-context) | Implemented: stream ID generation/parser/origin `dnsid/c2sp_tlog/lr.py:42`, `:51`, `:159`; logical event ID and signed bytes `dnsid/c2sp_tlog/event_codec.py:247`, `:259`; reader identity-instance rejection `:391`; writer rejection `dnsid/c2sp_tlog/prepared.py:312`. | Readers validate the signed FQDN against the stream ID; accepting an opaque reference syntactically does not bypass that check. The former bare-FQDN reader deviation is resolved. |
| Version-1 event envelope, JCS, supported event types, and size limits | [11: Event Envelope](../11-c2sp-tlog-binding.md#event-envelope) | Implemented: codec `dnsid/c2sp_tlog/event_codec.py:67`; parser `:130`; JCS `dnsid/c2sp_tlog/canonical.py:19`; size limit `dnsid/c2sp_tlog/event_codec.py:470`. | Signed bytes exclude only top-level signatures; unknown signed fields remain covered. |
| Corrected logical event identity and all-scope chaining | [11: Signed Context and Stream Chain](../11-c2sp-tlog-binding.md#signed-context-and-stream-chain), [11: Stream Completeness and Logged State](../11-c2sp-tlog-binding.md#stream-completeness-and-logged-state) | Implemented: signature-independent bytes and zero-byte-separated event IDs `dnsid/c2sp_tlog/event_codec.py:247`, `:259`; historical-authority selection, logical replay, and occurrence verification `dnsid/c2sp_tlog/stream_verifier.py:87`, `:222`; chain/state validation `dnsid/c2sp_tlog/stream_verifier.py:629`; focused regression `tests/test_c2sp_logical_identity.py:26`. | Corrected logical IDs deduplicate signature variants while distinct signed payloads and authenticated forks remain fatal; exact occurrence evidence is retained. |
| Lifecycle signer roles and signature verification | [11: Lifecycle Signatures](../11-c2sp-tlog-binding.md#lifecycle-signatures) | Implemented: roles `dnsid/c2sp_tlog/prepared.py:37`; signature requirements `dnsid/c2sp_tlog/event_codec.py:326`; lifecycle verification `dnsid/c2sp_tlog/stream_verifier.py:404`. | Includes binding-owned `NewOperational` proof. |
| Prepared event parse/sign/serialize/write surface | [11: Prepared Event Model](../11-c2sp-tlog-binding.md#prepared-event-model) | Partial: model/prepare `dnsid/c2sp_tlog/prepared.py:99`, `:122`; untrusted parse `:148`; role signing/identity-context checks `:177`, `:467`; complete bytes `:257`; safe write `:293`. | Non-ISSUANCE entity signing now requires expected FQDN and MIGRATION also pins `new_lr`; unknown signed fields remain preserved. Prepared events still lack corrected `eventId` metadata (Findings). |
| Submission adapter, durable idempotency, managed rotation, and stale-preparation behavior | [11: Submission and Idempotency](../11-c2sp-tlog-binding.md#submission-and-idempotency) | Implemented for the SDK contract: raw transport type `dnsid/models.py:1930`; abstract prepare/submit methods `dnsid/interfaces.py:553`, `:562`, `:575`; registry prepare/submit `dnsid/registry_client.py:846`, `:897`, `:978`; durable manager recovery `dnsid/manager.py:684`, `:828`, `:960`, `:1044`. | Registry preparation remains untrusted until binding parsing; exact bytes and idempotency keys survive retries. |
| Portable checkpoint/tile/entry read surface and source abstraction | [11: Read Surface](../11-c2sp-tlog-binding.md#read-surface) | Implemented: source/evidence interfaces `dnsid/c2sp_tlog/stream_source.py:64`, `:74`, `:89`; bounded scanner `:117`; consistency source `:107`; tile bundle parser `dnsid/c2sp_tlog/tiles.py:48`; portable bundle verifier `dnsid/c2sp_tlog/stream_bundle.py:91`, `:114`, `:265`; `ReadEvent` discovery and bundle-backed authorization `dnsid/c2sp_tlog/reader.py:597`, `:669`. | Standard entry-bundle geometry is fixed at 256. With bundle trust configured, `ReadEvent` verifies the exact requested occurrence under historical authority rather than substituting an earlier copy. |
| Global-log candidate selection and authenticated contradictions | [11: Read Surface](../11-c2sp-tlog-binding.md#read-surface), [11: Stream Completeness and Logged State](../11-c2sp-tlog-binding.md#stream-completeness-and-logged-state) | Implemented: selection/deduplication/authenticated failure `dnsid/c2sp_tlog/stream_verifier.py:87`, `:160`, `:185`, `:283`; stable categories `dnsid/c2sp_tlog/errors.py:12`; tests `tests/test_c2sp_tlog.py:521`, `:1111`, `:1134`. | Unauthenticated noise is ignored; authenticated chain, transition, and post-terminal contradictions are fatal. |
| Checkpoint, inclusion proof, append-only consistency, policy, and witness verification | [11: Checkpoint, Proof, and Policy Verification](../11-c2sp-tlog-binding.md#checkpoint-proof-and-policy-verification) | Implemented: proof `dnsid/c2sp_tlog/proof.py:66`; policy/quorum `dnsid/c2sp_tlog/policy.py:131`, `:178`; atomic checkpoint store `dnsid/c2sp_tlog/checkpoint_store.py:58`; durable `SQLiteCheckpointStore` `dnsid/c2sp_tlog/sqlite_checkpoint_store.py:20`; scan consistency `dnsid/c2sp_tlog/reader.py:903`, `:1146`; rollback/fork/inconsistency reported as typed `C2spCheckpointConsistencyError` with trusted and observed checkpoints `dnsid/c2sp_tlog/errors.py:63`; RFC 6962 consistency `dnsid/c2sp_tlog/merkle.py:52`; bundle consistency `dnsid/c2sp_tlog/stream_bundle.py:494`, `:536`. | Process-lifetime and injectable durable checkpoint stores are supported; verification never resets trust and re-baselining requires an explicit audited operation (`tests/test_sqlite_checkpoint_store.py:60`, `:70`). |
| Complete logged state, non-revocation, lifecycle ordering, and migration stitching | [11: Destination-Only Migration](../11-c2sp-tlog-binding.md#destination-only-migration), [11: Stream Completeness and Logged State](../11-c2sp-tlog-binding.md#stream-completeness-and-logged-state) | Implemented: evidence-returning freshness/non-revocation `dnsid/c2sp_tlog/reader.py:556`, `:585`; C2SP exact-cutoff implementation `:729`; recursive method dispatch and shared lifecycle/key checks `:935`, `:1016`, `:1040`; exact cutoff/resource/cycle validation `:1070`; tests `tests/test_c2sp_migration.py:546`, `:587`, `:608`, `:626`, `:649`. | Registered non-C2SP readers can return exact-cutoff verified history through `LogReader.rebuild_history_through`; C2SP retains ACTIVE/entity/operational continuity, cycle detection, and cumulative depth/event/response limits before stitching. |
| Standalone stream-bundle migration verification | [11: Destination-Only Migration](../11-c2sp-tlog-binding.md#destination-only-migration) | Partial: `dnsid/c2sp_tlog/stream_bundle.py:265`, `:398`; `tests/test_c2sp_migration.py:452`, `:436`. | Without a predecessor resolver the standalone API rejects migration genesis. Configured readers provide recursive verification (`tests/test_c2sp_migration.py:388`, `:436`). The design does not require a second standalone resolver API; this limitation does not downgrade those reader paths. |
| Scope-specific transport and trust policy | [11: Scope and Trust Policy](../11-c2sp-tlog-binding.md#scope-and-trust-policy) | Implemented: reference scope/HTTPS validation `dnsid/c2sp_tlog/lr.py:51`; policy rules `dnsid/c2sp_tlog/policy.py:69`, `:131`; explicit fetch guarantees/public enforcement `dnsid/c2sp_tlog/resource_fetcher.py:20`, `dnsid/c2sp_tlog/stream_source.py:271`; reader boundary `dnsid/c2sp_tlog/reader.py:392`. | Public sources must attest all required transport capabilities. |
| Verification convenience factory | [11: Verification Convenience Factory](../11-c2sp-tlog-binding.md#verification-convenience-factory) | Implemented: options/factory and migration limits `dnsid/c2sp_tlog/verification_registry.py:41`, `:95`, `:144`, `:197`; new `transport_config` (`:82`) is mutually exclusive with `resource_fetcher` (`:151`) and reaches the built-in fetcher unchanged, including `private_address_hosts` (`:158`, `dnsid/c2sp_tlog/resource_fetcher.py:54`, `:76`; `tests/test_c2sp_verification_registry.py:277`, `:298`), and malformed entries fail in the factory (`tests/test_ssrf_guard.py:285`); registry-wide predecessor dispatch `:231`; preferred-bundle availability fallback `dnsid/c2sp_tlog/stream_bundle.py:192`; exact bundle binding `:211`, `:311`; missing-consistency fallback `:226`; regression `tests/test_c2sp_migration.py:525`. | A completely read signed bundle with the wrong `lr` fails closed. Only listed availability failures and otherwise-valid bundles missing consistency evidence can use raw scanning; required-bundle mode disables both. |
| DNSid-managed trust factory | [11: DNSid-Managed Trust](../11-c2sp-tlog-binding.md#dnsid-managed-trust) | Implemented: reviewed development and production trust profiles and exact catalog selectors are at `dnsid/c2sp_tlog/managed_verification_registry.py:23`, `:33`, `:66`; opt-in construction, profile parsing, generic-registry reuse, shared infrastructure, fixed defaults, and exact dispatch are at `:80`, `:115`, `:123`, `:152`, `:157`. | Both catalog entries prefer verified stream bundles with the specified bounded raw-scan fallback. |
| C2SP error taxonomy | [11: Errors](../11-c2sp-tlog-binding.md#errors) | Implemented: categories/errors `dnsid/c2sp_tlog/errors.py:12`, `:39`, `:107`; HTTP/transport classification `dnsid/c2sp_tlog/resource_fetcher.py:85`; shared boundary adaptation `dnsid/c2sp_tlog/reader.py:224`, `:1448`; adversarial tests `tests/test_c2sp_tlog.py:1884`, `:1911`. | Unsafe transport, parse, policy, integrity, and limit failures are permanent; only specified availability failures are transient/fallback-eligible. |
| C2SP conformance and cross-SDK interoperability tests | [11: Conformance and Interoperability](../11-c2sp-tlog-binding.md#conformance-and-interoperability), [shared vectors](../conformance/README.md) | Implemented: production wire, split-signing, lifecycle-selection, trust-profile, managed-selection, recursive cross-method migration, standard-resource factory, preferred-bundle, portable-bundle, and central matrix tests pass at reviewed SDK commit `1257fd0` and design commit `1d3fbb5`. | `.venv/bin/pytest -q` passed 1285 tests; direct shared harness and matrix commands also passed. The matrix does not establish full recursive migration interoperability. |

### Protocol Data Types

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Identity record behavior profiles and dispatch | [07: Identity Record Behavior Profiles](../07-protocol-data-types.md#identity-record-behavior-profiles) | Implemented: selectors/default `dnsid/models.py:600`, `:613`; required tags `:620`; unsupported rejection `:695`; exact-selector canonicalization `:881`; verification dispatch `dnsid/manager.py:1501`. | Publishes only `dnsid-draft-01`; verifies it and exact pre-RFC `DNSid1`. |
| Parse / serialize / canonical / validate behavior | [07: DnsIdTxtRecord](../07-protocol-data-types.md#dnsidtxtrecord) | Implemented: parse `dnsid/models.py:679`; unsigned parse `:745`; validate `:819`; canonical `:881`; serialize `:902`. | Unknown syntactically valid tags are preserved and signed; `gi`, `ek`, and `ku` profile constraints are enforced. |
| FQDN normalization and `gi` helpers | [07: DnsIdTxtRecord](../07-protocol-data-types.md#dnsidtxtrecord) | Implemented: `normalize_fqdn` `dnsid/_utils.py:103`; `is_domain_name` `:154`; delegated `gi` is accepted structurally and bound through ISSUANCE `dnsid/c2sp_tlog/reader.py:461`. |  |
| `JWKS` and key constraints | [07: JWKS](../07-protocol-data-types.md#jwks) | Implemented: public serialization `dnsid/models.py:235`, `:308`; `JWKS` `:303`; `signature_alg` `:266`; current role keys `:345`, `:353`; role validation `:362`, `:366`; generic validation `:405`; distinctness helper `:468`. | Exactly one current signing key per draft 01 role with explicit consistent `alg` and `kid`; serializers return independent public-only dictionaries. |
| `VerifiedDomain` data type | [07: VerifiedDomain](../07-protocol-data-types.md#verifieddomain) | Implemented: core data/evidence `dnsid/models.py:1138`; cache-state, policy accessor, and expiry `:1187`, `:1195`, `:1199`; construction `dnsid/manager.py:1534`. | Runtime and record-signing evidence are retained, expiry includes both TLS bounds, and `requires_log_check()` exposes the operation-level policy signal without I/O. No trust flag or acceptance decision is stored (`tests/test_counterparty_acceptance.py:396`). |
| Wire version value and compatibility policy | [07: Version String Convention](../07-protocol-data-types.md#version-string-convention) | Implemented: `IDENTITY_RECORD_VERSION` and allow-lists `dnsid/models.py:605`, `:612`, `:613`; unsupported rejection `:695`. | Exact parsed selectors are never rewritten before signature verification. |
| SDK conformance metadata | [README: Conformance Metadata](../README.md#conformance-metadata) | Partial: immutable `SDK_CONFORMANCE` is at `dnsid/conformance.py:11`, `:37` and exported at `dnsid/__init__.py:90`; `known_deviations` is empty at `dnsid/conformance.py:47`. | The corrected method revision is pinned. Empty deviations no longer conflict with bare-FQDN reader behavior, but the prepared-event metadata and OIDC response-bound gaps below are not declared. |
| Owner-key selection / `sg` semantics | [07: Tags](../07-protocol-data-types.md#tags) | Implemented: `ek` selection/cardinality and canonical bytes `dnsid/manager.py:1499`, `:1506`; bare decode/verify `:1510`, `:1516`, `:1522`. | Detached JWS and algorithm-prefixed forms are rejected. |
| DNSid key service JWKS records | [07: DNSid Key Service JWKS Records](../07-protocol-data-types.md#dnsid-key-service-jwks-records) | Implemented: publication `dnsid/manager.py:428`, `:442`; verification `:1506`, `:1586`; role selectors `dnsid/models.py:345`, `:353`; public JSON serialization `:235`, `:308`. |  |
| TLS certificate representation and peer matching | [07: TLS Certificate](../07-protocol-data-types.md#tls-certificate) | Implemented: `TLSCertificate` and SAN-only matcher `dnsid/models.py:503`, `:515`; extraction `dnsid/_https_client.py:329`. | Wildcards match exactly one leftmost label; CN fallback is not used. |

### OIDC Federation

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| OIDC profile dependencies/configuration | [09: Dependencies](../09-oidc-federation.md#dependencies) | Implemented: `OIDCProfile` `dnsid/oidc.py:199`; `OIDCConfig` `:58`. |  |
| `OIDCProfile.from_identity_manager` | [09: Public Surface](../09-oidc-federation.md#public-surface) | Implemented: `dnsid/oidc.py:210`. |  |
| `CreateOIDCAssertion(opts)` | [09: CreateOIDCAssertion](../09-oidc-federation.md#createoidcassertionopts-oidcassertionoptions---string) | Implemented: `dnsid/oidc.py:271`; reserved claims including `fqdn` `:296`. |  |
| `DiscoverOIDCIssuer(issuer)` and issuer validation | [09: DiscoverOIDCIssuer](../09-oidc-federation.md#discoveroidcissuerissuer-string---oidcdiscoverydocument) | Implemented: discovery `dnsid/oidc.py:316`; exact issuer `:776`; same-origin endpoints `:820`; no redirects/body cap `:855`, `:940`. |  |
| `ExchangeOIDCToken` / `GetOIDCToken` | [09: ExchangeOIDCToken](../09-oidc-federation.md#exchangeoidctokenopts-oidctokenexchangeoptions---oidctokenresponse) | Partial: exchange `dnsid/oidc.py:383`; mint/get `:463`, `:478`; token-response cap/check `:886`, `:938`. | The token response is size-checked after buffered POST completion rather than bounded during reads; see Findings. |
| `VerifyOIDCToken(token, opts)` | [09: VerifyOIDCToken](../09-oidc-federation.md#verifyoidctokentoken-string-opts-verifyoidctokenoptions---verifiedoidcsubject) | Implemented: `dnsid/oidc.py:563`; issuer/audience/algorithm gates `:592`, `:598`, `:612`; current-peer DNSid subject verification `:660`; time checks `:1010`. | Subject verification inherits acceptance through the resolver; explicit `verify_dnsid_subject=False` (`dnsid/oidc.py:172`) yields no verified domain and is not a fallback. |
| OIDC response/data/error types | [09: Public Surface](../09-oidc-federation.md#public-surface) | Implemented: response/subject `dnsid/oidc.py:139`, `:176`; `NetworkError`/`OAuthError` `:39`, `:43`. |  |
| Optional `OIDCProvider` server surface | [09: Optional Provider Surface](../09-oidc-federation.md#optional-provider-surface) | Not applicable | Optional per design; no provider is implemented. |

### Web Bot Auth

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| WBA request signing | [10: Request signing](../10-web-bot-auth.md#request-signing) | Implemented: configuration bounds `dnsid/wba_signer.py:51`, `:71`; signing/content/component policy `:172`, `:211`, `:234`, `:243`. | Ed25519, thumbprint, nonce, tag, supplied empty content, and bounded lifetime match the profile. |
| `Signature-Agent` discovery | [10: Signature-Agent discovery](../10-web-bot-auth.md#signature-agent-discovery) | Implemented: discovery validation `dnsid/wba_signer.py:71`, `:149`; member-preserving update `:220`, `:286`. | Emits explicit `directory` or `jwks_uri` semantics. |
| Directory response | [10: Directory serving](../10-web-bot-auth.md#directory-serving) | Implemented: directory key conversion/serving `dnsid/wba_signer.py:314`, `:343`. | Covers required response components with bounded lifetime and canonical RFC 9421 metadata. |
| WBA verification | [10: Request verification requirements](../10-web-bot-auth.md#request-verification-requirements), [10: Directory verification requirements](../10-web-bot-auth.md#directory-verification-requirements) | Not applicable | RFC 9421 WBA verifier remains a future API. The separate JWT bot-token verifier now requires a trusted `expected_audience` and rejects missing/mismatched JWT `aud` (`dnsid/web_bot_auth.py:370`, `:398`, `:449`; `tests/test_web_bot_auth.py`). |

### Open Questions

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Track implementation choices affected by resolved design questions | [08: SDK Design Open Questions](../08-open-questions.md) | Not applicable: there are no unresolved shared questions. Implemented resolved choices include lifecycle reduction `dnsid/models.py:1270`, operation-level evidence `dnsid/manager.py:1669`, managed rotation `:684`, and C2SP selection/stitching `dnsid/c2sp_tlog/stream_verifier.py:87`, `dnsid/c2sp_tlog/reader.py:1340`. |  |

## Findings

| Design Item | Implementation Reference | Finding |
| --- | --- | --- |
| Standalone stream-bundle migration genesis | `dnsid/c2sp_tlog/stream_bundle.py:265`, `:398`; `tests/test_c2sp_migration.py:452`, `:436`; `dnsid/conformance.py:47` | **API limitation, not missing reader migration.** The standalone verifier has no predecessor resolver and fails closed. Configured readers verify predecessor histories recursively. No new standalone API is required solely to clear this row. |
| Bare-FQDN reference readers | `dnsid/c2sp_tlog/event_codec.py:391`; `dnsid/c2sp_tlog/prepared.py:312` | **Resolved.** Both reader signed-context validation and writers reject the identity FQDN as the stream ID. |
| Prepared C2SP event metadata | `dnsid/c2sp_tlog/prepared.py:99`, `:342` | **Partial.** Prepared events derive signed bytes locally but expose no `eventId` field/property required by the corrected prepared-event model. Add it before claiming complete prepared-event surface. |
| OIDC token-endpoint response limit | `dnsid/oidc.py:886`, `:938` | **Partial.** The token POST uses buffered `httpx.Client.post`; the maximum response size is checked only after buffering, not while reading. Use a bounded streamed POST before relying on this limit against untrusted issuers. |
| SDK conformance metadata | `dnsid/conformance.py:47` | **Partial.** `known_deviations` is empty despite the prepared-event metadata and OIDC response-bound gaps above. The old bare-FQDN reader finding is resolved. |
| Consolidated configuration and counterparty acceptance | `dnsid/manager.py:136`, `:1298`, `:1315`, `:1893`, `:1945`, `:1970` | **Resolved (`686d7bf`).** The `DnsidConfig`/`trusted_entities` contract is implemented and tested; former `Pre-redesign`/`TBD` rows are superseded. 0.19.1 resolved the unknown-field error type (`ArgumentError`) and retains the entity-key thumbprint on `VerifiedDomain`; `686d7bf` adds constructor-time type and value validation for `identity` URL/profile/`ka` fields and the `transport` string and list fields. |
| SSRF `.test` exemption | `dnsid/safe_transport.py:285`, `:114`; `tests/test_ssrf_guard.py:128` | **Resolved (`cd7a8ad`/`c434ad9`).** The implicit `.test` rule is removed; `private_address_hosts` implements design 05 `Private Address Hosts` with construction- and transport-time validation, loopback/private-use-only resolution, and C2SP passthrough. |
| Registration defaults vs design 05 | `dnsid/registry_client.py:202`, `:208` | **Resolved (design `5cc35bc`).** Design 05 `AgentRegistrationInput` was rewritten to the contract all three SDKs implement; the code is unchanged and now conforms. |
| Validation scope | `tests/` | `uv run pytest -q` (1376 tests), `uv run ruff check dnsid tests`, `uv run mypy dnsid`, 193 shared expectations and the C2SP matrix passed at `68ba790`. |
| Initial issuance recovery | `dnsid/manager.py:428`, `:442`, `:460`; `dnsid/c2sp_tlog/prepared.py:119` | **Evidence incomplete.** Publication/signing primitives and registry transport do not demonstrate a durable end-to-end issuance/recovery composition. Identify and test its coordinator before marking the whole flow Implemented. |

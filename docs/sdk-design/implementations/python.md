# Python SDK Implementation Coverage

## Analysis Details

| Field | Value |
| --- | --- |
| SDK repository | dnsid-py |
| SDK branch | `main` |
| SDK commit | `1257fd0b44fd3b0e214ba5bc7db427b5c266c91e` |
| SDK package version | `0.19.1` |
| dnsid design docs commit | `1d3fbb551201669bd066b3f543f4f571622377e5` |
| Analysis date | 2026-09-15 |
| Validation commands | `.venv/bin/pytest -q` passed at the reviewed head (1285 tests); shared harness 193/193 expectations and the C2SP matrix (9 interoperable paths, 6 mutated bundles rejected) passed at this head via `harness/run.py` and `harness/c2sp_matrix.py`. |

## Review scope — 2026-09-15

Follow-up (same day): reviewed `8490ca4`..`1257fd0` (three commits: `96b14a2` config-object unknown/missing fields raise `ArgumentError`, `VerifiedDomain.signing_key_thumbprint` retained and used by acceptance, `private_address_hosts` gains leading-dot suffix entries, profile docstrings on inherited transport; NOTICE copyright line; `0.19.1` release). Re-reviewed the configuration-validation, pin, `VerifiedDomain`, SSRF/transport, and error rows; line references refreshed in `dnsid/models.py`, `dnsid/safe_transport.py`, `dnsid/oidc.py`, and tests. Suites rerun at `1257fd0`.

Initial review: SDK commits `2fc0e9e`..`8490ca4` (seven commits: durable SQLite checkpoint store with typed consistency errors, atomic key-store persistence under a transaction lock, a dependency bump, thumbprint-derived kids plus registry `id`/`JWK.from_dict`/`cancel_agent`/typed registry errors, the `DnsidConfig` consolidation with counterparty acceptance (`f5b3080`), peer-certificate extraction from httpx's network stream, and the `0.19.0` release) from the recorded `53d4d60` baseline, and design commits `73d5aa1`, `dac6575`, `59a2696`, `7f9d681`, `664e281`, and `e773433` from `dd5ef3b`. Both baselines resolve and are ancestors of the reviewed heads; no root `fixtures/` or `conformance/` files changed, and there were no uncommitted design or SDK changes. The consolidated configuration/acceptance contract is a public API redesign, so every `Pre-redesign` and `TBD` row was re-reviewed against the settled design; rows whose cited files changed had line references refreshed, and rows whose design requirements and implementation files were both untouched are preserved. The former `Pre-redesign` marker is retired. Previously recorded gaps (prepared-event `eventId`, OIDC buffered token response, conformance-metadata declarations, issuance-recovery evidence) are unchanged by this delta and remain open.

## Coverage Checklist

### Document Set and Shared Contracts

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Document set and profile boundaries | [README: Document Set](../README.md#document-set) | Implemented: core `dnsid/manager.py:121`; draft 01 publication `dnsid/manager.py:453`, `:507`; profiles `dnsid/jose.py:64`, `dnsid/http_signatures.py:79`, `dnsid/oidc.py:199`; WBA signer/directory `dnsid/wba_signer.py:107`, `:306`; transport `dnsid/registry_client.py:51`; shared logs `dnsid/registry.py:16`; C2SP binding `dnsid/c2sp_tlog/reader.py:248`. | Numbered draft publication, pre-RFC `DNSid1` verification, local entity-key management, and WBA directory serving are present. |
| Shared profile contracts: identity verification, stable IDs, logs, errors, defaults | [README: Shared Profile Contracts](../README.md#shared-profile-contracts) | Implemented: `IdentityResolver` and peer certificate `dnsid/interfaces.py:41`; `{domain}#{kid}` parsing `dnsid/_utils.py:169`; split `ek`/`ku` verification and mandatory lifecycle checks `dnsid/manager.py:1521`, `:1554`, `:1571`; defaults/selectors `dnsid/models.py:56`, `:589`, `:602`; operation-level evidence `dnsid/models.py:1099`, `:1184`, `dnsid/manager.py:1662`; status freshness/fail-closed behavior `dnsid/manager.py:1320`, `:1739`. | Successful verification is fail-closed, profile verifiers propagate current-peer evidence, and operation-level log checks retain structured evidence. |
| Dependency direction between core, profiles, transport, registry, and logs | [README: Dependency Direction](../README.md#dependency-direction) | Implemented: profiles depend on `IdentityResolver`/`KeyProvider` (`dnsid/http_signatures.py:46`, `dnsid/jose.py:48`, `dnsid/oidc.py:27`); registry client is concrete transport (`dnsid/registry_client.py:51`); C2SP depends on shared interfaces/types (`dnsid/c2sp_tlog/reader.py:15`). |  |
| SDK naming conventions | [README: Naming](../README.md#naming) | Implemented: snake_case public methods including `verify_domain` `dnsid/manager.py:1244`, `create_txt_record` `:453`, and `get_key_set` `:421`. |  |

### SDK Configuration and Counterparty Acceptance

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| `DnsidConfig` single entry point: `identity?`, `verification`, `transport` sections; data-only config, runtime objects in deps | [01: Initialization](../01-core-identity-manager.md#initialization) | Implemented: `DnsidConfig`/`IdentityConfig`/`TrustedEntity`/`VerificationConfig`/`TransportConfig` at `dnsid/models.py:174`, `:56`, `:100`, `:120`, `:143`; single constructor `IdentityManager(config, key_provider, deps)` at `dnsid/manager.py:130`; runtime objects live in `IdentityManagerDependencies` (`dnsid/manager.py:85`, entity provider `:118`), which no longer carries `transport_config`. | `ProtocolConfig`, `transport_config` on deps, and the dummy-domain `for_verification` arguments were removed; a non-`DnsidConfig` root is rejected with `ArgumentError` (`dnsid/manager.py:160`). |
| Verification-only manager by omitting `identity`; key providers rejected with `ArgumentError`; idiomatic alias optional | [01: Initialization](../01-core-identity-manager.md#initialization) | Implemented: `identity is None` with `key_provider` or `deps.entity_key_provider` raises `ArgumentError` at `dnsid/manager.py:168`; `identity` without `key_provider` raises at `:174`; `for_verification(deps, verification=, transport=)` delegates to the constructor at `dnsid/manager.py:223`; local-only operations guard through `_local_identity` at `:255`. | Tests `tests/test_manager.py:130`, `:144`, `:151`, `:155`, `:162`, `:169`. |
| `IdentityConfig` contains only publication settings | [01: IdentityConfig](../01-core-identity-manager.md#identityconfig) | Implemented: `dnsid/models.py:56` carries only publication fields; `status_check_interval`/`dnssec_mode` were removed. | Unknown keyword fields are rejected by the dataclass constructor (`tests/test_counterparty_acceptance.py:174`). |
| `VerificationConfig`: `statusCheckInterval`, `dnssecMode`, `trustedEntities?` with defaults | [01: VerificationConfig](../01-core-identity-manager.md#verificationconfig) | Implemented: `dnsid/models.py:120` defaults to zero interval, `AUTO`, and `None` allowlist; `_validate_verification_config` rejects non-`timedelta`/negative intervals and non-`DNSSECMode` values at `dnsid/manager.py:1887`; the section is consumed identically in both modes at `dnsid/manager.py:1449`, `:1370`. | Test `tests/test_counterparty_acceptance.py:113`. |
| Construction validates and snapshots config before network work; unknown fields, invalid values, duplicate normalized entities, invalid pins rejected | [01: Configuration Ownership and Loading](../01-core-identity-manager.md#configuration-ownership-and-loading) | Partial: the constructor deep-copies the config (`dnsid/manager.py:164`) and validates before building any transport (`:177`–`:181`); `_validate_verification_config` type-checks every verification field and normalizes/rejects invalid or duplicate `governance_id`, string or empty pin lists, duplicate pins, and non-32-byte strict-base64url pins, returning a frozen tuple snapshot at `dnsid/manager.py:1887`; `_validate_transport_config` checks the section type at `:1938`; `config` returns an independent copy at `:243`. Unknown or missing fields on every config dataclass now raise `ArgumentError` at config-object construction via `_config_errors_are_argument_errors` (`dnsid/models.py:39`, applied at `:55`, `:99`, `:119`, `:142`, `:173`). Missing: `identity` fields other than `domain`/`governance_id` (`ek_url`, `ku_url`, `status_url`, `publish_profile`, `max_key_age`) and the `transport` string fields are not type-checked at construction; mistyped values surface only when a record is built or the transport is used. | Python dataclasses can carry mistyped values, so the design requires constructor-time rejection. Tests `tests/test_counterparty_acceptance.py:126`, `:165`, `:174`. |
| Injected-transport conflicts: `dnsServer` configures whichever default consumer is not injected, rejected when both are; `caBundlePath` rejected when HTTPS fetcher is injected | [01: Configuration Ownership and Loading](../01-core-identity-manager.md#configuration-ownership-and-loading) | Implemented: `_validate_transport_config` rejects `dns_server` only when both `dns_resolver` and `https_fetcher` are injected and `ca_bundle_path` when `https_fetcher` is injected at `dnsid/manager.py:1938`; defaults are built only for non-injected consumers at `dnsid/manager.py:193`, `:196`. Injected objects are not inspected. | Tests `tests/test_counterparty_acceptance.py:186`, `:196`, `:204`. |
| Explicit loaders only; precedence rule (caller wins, omission ≠ empty, arrays replace); no inference of trust from local identity data | [01: Configuration Ownership and Loading](../01-core-identity-manager.md#configuration-ownership-and-loading) | Implemented: the constructor reads no files or environment; `config_from_environment` is an explicit loader returning `DnsidConfig` plus a separate `RegistryConfig` at `dnsid/environment.py:119`, `:198`; the CLI loader overlays non-empty caller `identity` fields before normalization/derivation and takes `verification`/`transport` only from the caller at `dnsid/cli_config.py:137`, `:168`, `:169`. | Tests `tests/test_cli_config.py:643`, `:720`, `:740`, `:748`. |
| Deployment file: `{ dnsid, logTrust }` shape, 1:1 section mapping, exactly one `logTrust` variant | [01: Deployment File](../01-core-identity-manager.md#deployment-file) | Not applicable | Python ships no verifier file loader; the design binds the shape only to bindings that offer one. |
| CLI loader maps flat snake_case fields into `config.identity`; verification settings never inferred from publication data | [01: Initialization from DNSid CLI Configuration](../01-core-identity-manager.md#initialization-from-dnsid-cli-configuration) | Implemented: `config_from_cli_directory(path, config)` at `dnsid/cli_config.py:37` maps snake_case/camelCase publication fields into `IdentityConfig` (`:118`), overlays caller values (`:137`), derives `status_url` only when absent (`:153`), and `identity_manager_from_cli_directory` loads keys by the effective domain and delegates to the constructor (`:178`, `:211`, `:217`). | Former `dnssec_mode`/transport reads from CLI files are gone (`tests/test_cli_config.py:643`). |
| `trustedEntities` exact `governanceId` match via `GovernanceID(record)`; no wildcard/suffix/transitive matches; `[]` denies all; absent makes no decision | [01: Counterparty Acceptance](../01-core-identity-manager.md#counterparty-acceptance), [07: Identity Record Behavior Profiles](../07-protocol-data-types.md#identity-record-behavior-profiles) | Implemented: `_enforce_counterparty_acceptance` at `dnsid/manager.py:1329` returns without a decision for `None`, compares each normalized configured `governance_id` exactly against the signed `record.gi`, and denies otherwise; an empty tuple denies all. | Tests `tests/test_counterparty_acceptance.py:232`, `:237`, `:243` (unrelated delegated agent domain), `:258`. |
| `entityKeyThumbprints` pins: canonical RFC 7638 SHA-256 thumbprints of the current record-signing key; no automatic pin learning or rotation expansion | [01: Counterparty Acceptance](../01-core-identity-manager.md#counterparty-acceptance), [05: Registry Custody and Pins](../05-transport-and-registry.md#registry-custody-and-pins) | Implemented: pins are compared against `result.signing_key_thumbprint` (the verified `ek` key) at `dnsid/manager.py:1335`, `:1339`; the thumbprint is computed once and retained on `VerifiedDomain` as a cached property (`dnsid/models.py:1171`); the operational key is never consulted; no code path adds pins. | Tests `tests/test_counterparty_acceptance.py:265` (asserts no recomputation), `:280`. |
| Acceptance ordering in `VerifyDomain`: after all protocol and current-peer checks, before final freshness check, on fresh lookups, status refreshes, and cache hits | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: the shared boundary `_verify` at `dnsid/manager.py:1312` runs `_reusable_verification` (fresh, coalesced, cache hit, or refresh), then `_enforce_mtls_policy` (`:1323`), then acceptance (`:1325`), then `_require_fresh_evidence` (`:1326`), for every invocation. | Tests `tests/test_counterparty_acceptance.py:322`, `:335`, `:349`. |
| Evidence cached before acceptance; denial and decision never cached; acceptance policy part of the verification context | [01: IdentityCache](../01-core-identity-manager.md#identitycache) | Implemented: fresh and refreshed evidence is `put` inside the reusable path at `dnsid/manager.py:1635`, `:1440` before acceptance runs; the acceptance check only raises (`:1329`); every manager wraps a supplied backend in a private namespace at `dnsid/manager.py:203`, `:2234`. | Tests `tests/test_counterparty_acceptance.py:322`, `:335`, `:375`. |
| `CounterpartyNotAccepted` permanent code with `VerifiedGovernanceID` and `VerifiedEntityKeyThumbprint`; no disclosure of configured allowlist or pins | [01: VerificationError](../01-core-identity-manager.md#verificationerror) | Implemented: `VerificationCode.COUNTERPARTY_NOT_ACCEPTED` at `dnsid/enums.py:77`; `verified_governance_id`/`verified_entity_key_thumbprint` on `VerificationError` at `dnsid/exceptions.py:97`, `:122`; the denial is raised non-transient with only observed values at `dnsid/manager.py:1346`. | Test `tests/test_counterparty_acceptance.py:291`. |
| `VerifiedDomain` carries no trust flag or cached acceptance decision | [07: VerifiedDomain](../07-protocol-data-types.md#verifieddomain) | Implemented: `dnsid/models.py:1127` holds protocol evidence only; `signing_key_thumbprint` (`:1171`) is derived evidence, not a decision. | Test `tests/test_counterparty_acceptance.py:307`. |
| Registry-managed publication confirmation uses the protocol-only internal path; public `VerifyDomain(localDomain)` still enforces acceptance | [05: AwaitRegistryManagedPublication](../05-transport-and-registry.md#awaitregistrymanagedpublicationregistryclient-registryclient---publishedrecord) | Implemented: `await_registry_managed_publication` calls the private `_verify_publication_evidence` at `dnsid/manager.py:666`, which passes `acceptance=False` (`:1308`); the only public entry point `verify_domain` passes `acceptance=True` (`:1244`, `:1306`). | Test `tests/test_counterparty_acceptance.py:397`. |
| Profile verifiers (JOSE, HTTP signatures, OIDC subject, WBA) inherit acceptance through the resolver; issuer-only OIDC and generic WBA make no acceptance claim and are never a fallback | [03: Profile Configuration](../03-jose-jwt-jws.md#profile-configuration), [09: VerifyOIDCToken](../09-oidc-federation.md#verifyoidctokentoken-string-opts-verifyoidctokenoptions---verifiedoidcsubject), [10: Configuration](../10-web-bot-auth.md#configuration) | Implemented: JOSE, HTTP signature, OIDC subject, and the JWT bot-token profile resolve identities through `IdentityResolver.verify_domain` (`dnsid/jose.py:267`, `:378`, `dnsid/http_signatures.py:551`, `dnsid/oidc.py:660`, `dnsid/web_bot_auth.py:541`), inheriting acceptance per invocation; local-identity fields come from `manager.local_domain` and profile settings stay profile-owned. OIDC `verify_dnsid_subject=False` is explicit and yields no verified domain (`dnsid/oidc.py:172`, `:660`). | Python ships no RFC 9421 WBA verifier (see Web Bot Auth), so the generic-mode rule is not applicable. |
| Required validation matrix for configuration and acceptance scenarios | [01: Counterparty Acceptance](../01-core-identity-manager.md#counterparty-acceptance) | Implemented: `tests/test_counterparty_acceptance.py:113` (same defaults), `:126` (snapshot), `:165`/`:174` (invalid/unknown before network), `:186`–`:204` (transport matrix), `:232`/`:237` (omitted vs empty), `:243` (exact `gi`, unrelated agent), `:258` (child/suffix/lookalike), `:265`/`:280` (pins vs `ku`/other), `:291` (non-disclosure), `:307` (no trust flag), `:322`/`:335` (fresh denial and refresh keep evidence cached), `:349` (concurrent), `:375` (shared backend isolation), `:397` (publication vs public local verify); expiry-before-return `tests/test_corrected_boundaries.py:121`. | The deployment-file row is Not applicable for Python. |

### Core Identity Manager

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| `IdentityManager` type / facade | [01: IdentityManager](../01-core-identity-manager.md#identitymanager) | Implemented: `dnsid/manager.py:121`; local constructor `:130`; discoverable keyless `for_verification` `:223`; local mutation/signing guards `:407`, `:415`. | Verification-only construction requires no caller-supplied dummy identity or key provider. |
| Initialization config validation and dependency wiring | [01: Initialization](../01-core-identity-manager.md#initialization) | Implemented: config validation/snapshot and dependency wiring `dnsid/manager.py:130`, `:164`, `:177`; dependency type `:85`; entity provider `:118`, `:189`; local log binding derivation `:210`. | Separate operational and entity providers are wired; see the configuration/acceptance section for the full contract. |
| Configuration / options surface | [01: Initialization](../01-core-identity-manager.md#initialization) | Implemented: `IdentityConfig` with `ek_url`/`ku_url`/`publish_profile` `dnsid/models.py:56`, `:91`; `VerificationConfig`/`TrustedEntity` `:120`, `:100`; `TransportConfig` with hostname-scoped private-address policy (exact or leading-dot suffix entries) `:143`, `:169`; `DnsidConfig` `:174`; `RegistryConfig` `:194`; public snapshot accessor `dnsid/manager.py:243`. | Configuration is validated and snapshotted at construction; registry settings are owned by the registry client. |
| Environment-based configuration loader | [01: Initialization](../01-core-identity-manager.md#initialization) | Implemented: explicit config loader `dnsid/environment.py:119`; `DnsidConfig` identity/verification/transport construction `:198`, `:208`, `:209`; registry config `:215`; publish profile `:206`; one-call manager loader `:81`. | Explicit loader only; constructors never read the environment. |
| CLI configuration loader | [01: Initialization from DNSid CLI Configuration](../01-core-identity-manager.md#initialization-from-dnsid-cli-configuration) | Implemented: `config_from_cli_directory` `dnsid/cli_config.py:37`; `DNSID_CONFIG_DIR` precedence `:94`; publication field mapping, caller overlay, and entity key `:118`, `:137`, `:148`, `:155`; manager/provider wiring `:178`, `:211`, `:217`; JWK/PEM loading `dnsid/local_key_provider.py:432`, `:485`. | Consumes persisted publication values without inventing protocol defaults; an explicitly supplied entity provider wins. Superseding detail is in the configuration/acceptance section. |
| Local log signing/write split (`CanonicalizeLogEvent`, `RequiredLogSignatures`, `SignEvent`, `WriteSignedEvent`, `SignAndWriteEvent`) | [01: Core Methods](../01-core-identity-manager.md#core-methods) | Implemented: `dnsid/manager.py:266`, `:289`, `:312`, `:345`, `:370`; provider-explicit primitive `:1811`; role table `:1855`. | Entity and operational roles route to separate providers; canonicalization excludes signature fields. |
| `GetKeySet()` | [01: GetKeySet](../01-core-identity-manager.md#getkeyset---jwks) | Implemented: `dnsid/manager.py:421`. | Returns exactly the current operational key. |
| `GetEntityKeySet()` | [01: GetEntityKeySet](../01-core-identity-manager.md#getentitykeyset---jwks) | Implemented: `dnsid/manager.py:435`. | Returns exactly the current entity key and fails when no entity provider is configured. |
| `CreateTxtRecord()` | [01: CreateTxtRecord](../01-core-identity-manager.md#createtxtrecord---string) | Implemented: `dnsid/manager.py:453`; unsigned builder/default selector `:2177`; key distinctness `:487`, `:500`. | Emits exact `v=dnsid-draft-01`, uses the entity provider, and rejects pre-RFC `DNSid1` publication. |
| `VerifyDomain(domain, peerCert?)` end-to-end flow | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: `dnsid/manager.py:1244`; parse/validate `:1500`, `:1511`; `ek` and `ku` fetch `:1521`, `:1554`; lifecycle checks `:1578`; per-caller mTLS `:1323`; status `:1601`; result evidence `:1618`. | Authenticates `sg` before following `su` or loading log evidence. Both selectors preserve exact `v=` bytes and require bilateral binding plus continuity. Independent post-signature work is serial; bounded concurrency is only a SHOULD. |
| DNS TXT lookup at `_dnsid.{domain}` | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: `dnsid/manager.py:1453`. |  |
| TXT record parsing and exactly-one-record validation | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: cardinality `dnsid/manager.py:1484`; parser `dnsid/models.py:668`; exact selector allow-list `:602`; unsupported-selector rejection `:684`. | Unknown future selectors fail closed without rewriting the wire value. |
| JWKS fetch and bounded response handling | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: split fetches `dnsid/manager.py:1521`, `:1554`; initial/redirect URL checks and streamed exact-200 1 MiB handling `dnsid/_https_client.py:33`, `:49`, `:58`, `:67`, `:73`; checked-IP transport `dnsid/safe_transport.py:46`, `:79`; signed-record URL validation `dnsid/models.py:808`, `:2149`. | The default helper rejects credentials and fragments before fetching, validates each redirect, and enforces its decoded cap while streaming. |
| Status fetch and active status validation | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: full and refreshed status checks are at `dnsid/manager.py:1600`, `:1429`; transient `StatusUnavailable` remapping and semantic-error wrapping are at `:1739`, `:1748`; streamed parsing is at `dnsid/_https_client.py:119`, `:205`; strict type/state validation is at `dnsid/models.py:563`. | Transport failure is indeterminate and never returns stale success; malformed documents become non-transient `VerificationError(RecordInvalid)` with a chained `ValidationError`, and valid non-ACTIVE status is rejected. |
| `AgentStatus` validation and workflow-state separation | [07: AgentStatus](../07-protocol-data-types.md#agentstatus), [06: Implemented lifecycle interpretation](../06-log-bindings.md#implemented-lifecycle-interpretation) | Implemented: model/validation `dnsid/models.py:544`, `:563`; authoritative nested protocol-status parsing `dnsid/_https_client.py:205`; ACTIVE-only verification `dnsid/manager.py:1603`; tests `tests/test_https_client.py:26`, `:39`, `:69`. | Accepts only the six exact protocol states and does not reinterpret registry workflow states. |
| Signature validation against JWKS keys | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: single `ek` key and canonical bytes `dnsid/manager.py:1523`, `:1527`; bare-signature parse/verify `:1528`, `:1534`, `:1540`; operational key and distinctness `:1554`, `:1558`. | Draft 01 requires explicit consistent JWK `alg`, one current key per role, and bare unpadded base64url `sg`. |
| Policy flag enforcement, including mTLS behavior | [01: VerifyDomain](../01-core-identity-manager.md#verifydomaindomain-string-peercert-tlscertificate---verifieddomain) | Implemented: per-caller enforcement after reusable verification `dnsid/manager.py:1322`, `:1323`; RFC 9525-style SAN matcher `dnsid/models.py:504`; profile propagation `dnsid/jose.py:267`, `dnsid/http_signatures.py:551`, `dnsid/oidc.py:655`. | `fl=mtls` checks the current invocation's peer certificate before every successful return, including fresh and stale cache hits. |
| Operation-level log evidence (`RequiresLogCheck`, `VerifyLogEvidence`) | [01: VerifyLogEvidence](../01-core-identity-manager.md#verifylogevidencevd-verifieddomain-at-timestamp---loggedstateevidence) | Implemented: evidence model `dnsid/models.py:1099`; policy accessor `:1184`; manager helper/default operation time `dnsid/manager.py:1662`; evidence-returning interface `dnsid/interfaces.py:236`; C2SP verification and evidence construction `dnsid/c2sp_tlog/reader.py:546`, `:575`. | The helper remains operation-level, does not refresh protocol status, and returns the accepted stitched history bounds, completeness mode, checkpoint, and freshness time. |
| Cache read/write and freshness behavior | [01: IdentityCache](../01-core-identity-manager.md#identitycache) | Implemented: cache hit, per-call mTLS, coalesced miss/status refresh, shared failure, stale-error rejection, and copy-before-update `dnsid/manager.py:1323`, `:1365`, `:1376`, `:1412`, `:1428`; thread-safe `_InMemoryCache` `:2260`; expiry including `ek` TLS `dnsid/models.py:1188`. | Verification failures are not cached; same-domain reusable work is coalesced while different domains proceed independently. Eligible evidence is cached before acceptance and denials are never cached (`tests/test_counterparty_acceptance.py:322`). |
| Domain-log loading and cache eviction | [01: LoadDomainLog](../01-core-identity-manager.md#loaddomainlogvd-verifieddomain---domainlog), [01: EvictDomain](../01-core-identity-manager.md#evictdomaindomain-string) | Implemented: eviction `dnsid/manager.py:1680`; loading `:1684`; lifecycle replay `dnsid/c2sp_tlog/reader.py:708`. |  |
| Error types and verification codes | [01: Error Types](../01-core-identity-manager.md#error-types) | Implemented: error classes `dnsid/exceptions.py:22`; codes `dnsid/enums.py:46`; `COUNTERPARTY_NOT_ACCEPTED` and observed-identity fields `dnsid/enums.py:77`, `dnsid/exceptions.py:97`; typed registry request errors `dnsid/exceptions.py:158`; lifecycle categories/errors `dnsid/enums.py:117`, `dnsid/exceptions.py:132`; live status wrapping `dnsid/manager.py:1739`, `:1748`. | Live failures preserve structured code/transient/state fields and chained causes; parse and validation failures encountered during verification are adapted to non-transient verification errors. `RegistryRequestError` keeps the `LOG_ERROR` category, exposes HTTP status and registry error code, and never echoes response bodies. |
| `KeyProvider` interface and local/cloud providers | [01: KeyProvider Interface](../01-core-identity-manager.md#keyprovider-interface) | Implemented: interface `dnsid/interfaces.py:80`; `sign_key` `:130`; `supersede` `:160`; local provider `dnsid/local_key_provider.py:313`, `:527`, `:605`; atomic write under an exclusive file lock `:139`, `:165`; AWS provider `dnsid/aws_kms_key_provider.py:259`, `:441`. | Local EdDSA/ES256 generation and rotation are supported; the key store is persisted atomically (temp file, fsync, rename) under a transaction lock and generated kids are RFC 7638 thumbprints (`tests/test_local_key_provider.py:132`, `tests/test_generated_kid_is_thumbprint.py`). |
| Key generation, activation, supersede, rotation flow | [01: Operational Key Rotation](../01-core-identity-manager.md#operational-key-rotation), [01: Managed Rotation Recovery Contract](../01-core-identity-manager.md#managed-rotation-recovery-contract) | Implemented: managed rotation `dnsid/manager.py:685`; durable signing/persist/pause ordering `:782`, `:788`, `:805`, `:816`; resume `:833`; accepted reconciliation `:1053`; persisted-state validation `:1122`; mandatory hooks `:1199`. | Exact prepared bytes and state are persisted before submission and through every recoverable transition. |
| `HTTPSFetcher` interface and secure HTTP defaults | [01: HTTPSFetcher Interface](../01-core-identity-manager.md#httpsfetcher-interface) | Implemented: interface `dnsid/interfaces.py:447`; streamed exact-200 JWKS/status helpers and URL checks `dnsid/_https_client.py:33`, `:49`, `:119`, `:130`, `:261`; checked-resolution transport and exact/suffix host allowlist `dnsid/safe_transport.py:46`, `:63`, `:204`, `:79`. | TLS, finite timeout, redirect count, streaming decoded-size enforcement, exact HTTP 200, credential/fragment rejection, DNS-rebinding-safe routing, and hostname-scoped private-service access are present. |
| HTTPS timeout, redirect, CA bundle, body-size limits | [01: HTTPSFetcher Interface](../01-core-identity-manager.md#httpsfetcher-interface) | Implemented: finite constants `dnsid/_https_client.py:19`; redirect checks `:261`, `:284`; CA augmentation `dnsid/safe_transport.py:87`; streaming body checks `dnsid/_https_client.py:73`, `:159`, `:243`. | JWKS and status decoded-body limits are enforced during reads and share the invocation budget. |
| SSRF-safe HTTP transport / fetch guard | [01: HTTPSFetcher Interface](../01-core-identity-manager.md#httpsfetcher-interface) | Implemented: all-address validation, exact/leading-dot-suffix host allowlisting, and checked-IP connection `dnsid/safe_transport.py:46`, `:63`, `:204`, `:122`; configuration `dnsid/models.py:143`, `:169`; pooled core transport `dnsid/manager.py:196`, `:1710`, `:1737`; OIDC reuse `dnsid/oidc.py:833`, `:842`. | Public reads fail closed on any non-global result unless the normalized hostname matches an explicitly configured exact entry or `.suffix` entry; suffix matching is label-bounded (`evildnsid.test` does not match `.dnsid.test`; `tests/test_ssrf_guard.py:77`). The design requires a hostname-scoped opt-in allowlist; suffix entries remain hostname-scoped and opt-in. |
| `DNSResolver` interface and default/system/custom/DoH behavior | [01: DNSResolver Interface](../01-core-identity-manager.md#dnsresolver-interface) | Implemented: interface `dnsid/interfaces.py:426`; default resolver `dnsid/_default_resolver.py:17`; standard/custom DNS `:50`; DoH `:98`; secure-channel AD handling `:153`; system resolver `:176`; manager default `dnsid/manager.py:1955`. | Only explicit HTTPS DoH may map AD to `VALID`; system, ordinary DNS, and plain HTTP report `UNKNOWN`. |
| DNS TTL capture and cache TTL use | [01: DNSResolver Interface](../01-core-identity-manager.md#dnsresolver-interface) | Implemented: `TXTRecord.ttl` `dnsid/models.py:922`; cache expiry `dnsid/models.py:1188`. |  |
| DNSSEC state and enforcement modes | [01: DNSSECState](../01-core-identity-manager.md#dnssecstate) | Implemented: enums `dnsid/enums.py:12`, `:29`; strict policy checks `dnsid/manager.py:1449`, `:1462`, `:1467`, `:1476`; tests `tests/test_verify_domain.py:431`, `:484`. | `AUTO` preserves `UNKNOWN`; `VALIDATED` rejects `UNKNOWN`; `REQUIRED` accepts only `VALID`; `FAILED` always aborts. |
| Initial setup local identity flow | [01: Initial Setup](../01-core-identity-manager.md#initial-setup-of-local-identity) | Partial: publication key sets and record `dnsid/manager.py:421`, `:435`, `:453`; ISSUANCE signer roles `:1855`; C2SP split preparation `dnsid/c2sp_tlog/prepared.py:119`. | Key/signing primitives are established; these citations do not substantiate durable initial-issuance intent, restart recovery, exact-byte retries, acceptance binding, or idempotent completion. Registry coordination alone does not prove SDK/caller recovery. This is an evidence gap, not proof that every supported composition lacks it. |
| Registry status change / revocation flow | [01: Registry Status Change](../01-core-identity-manager.md#registry-status-change-eg-revocation) | Implemented: owner-authorized typed revoke request with immutable agent ID `dnsid/registry_client.py:305`; reason namespace `dnsid/enums.py:105`. | The registry is the sole append/retry coordinator; obsolete client-prepared later-event APIs were removed. |
| Retry / backoff behavior for transient failures | [01: VerificationError](../01-core-identity-manager.md#verificationerror) | Implemented: sync/async retry helpers `dnsid/retry.py:13`, `:54`. |  |
| Core non-goals honored | [01: Non-Goals](../01-core-identity-manager.md#non-goals) | Implemented: core does not publish DNS/certificates/blockchain; registry control plane is separate (`dnsid/registry_client.py:51`). |  |

### Verification Boundaries

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Shared finite verification budget | [README: Verification Resource Budgets](../README.md#verification-resource-budgets) | Implemented: public context manager/decorator `dnsid/_verification_budget.py:53`, `:100`; independent waiter timeout `:79`; socket/TLS remaining-time enforcement `dnsid/safe_transport.py:144`; core `dnsid/manager.py:1243`; profiles `dnsid/jose.py:188`, `dnsid/http_signatures.py:363`, `dnsid/oidc.py:562`; C2SP boundary `dnsid/c2sp_tlog/reader.py:224`. | Default 30 seconds, nested operations cannot extend it; cancellation is cooperative and injected blocking dependencies must honor remaining time. |
| Cache isolation, acquisition-based expiry, and zero TTL | [01: IdentityCache](../01-core-identity-manager.md#identitycache) | Implemented: configuration snapshots/private backend `dnsid/manager.py:158`, `:203`; DNS acquisition `:1452`; return-time evidence checks `:1638`; cache wrappers `:2234`, `:2260`; absolute expiry `dnsid/models.py:1188`; regression `tests/test_corrected_boundaries.py:121`, `:163`. | Status refresh cannot revive expired evidence; zero-TTL results are not cached or reused by waiting callers. Trust or acceptance-policy changes require a new manager; the private per-manager namespace already isolates acceptance contexts (`tests/test_counterparty_acceptance.py:375`). |
| Strict JOSE/OIDC parsing, dates, presence defaults, and expected audience | [03: Parsing and validation boundaries](../03-jose-jwt-jws.md#parsing-and-validation-boundaries), [09: Profile Configuration](../09-oidc-federation.md#profile-configuration) | Implemented: bounded strict compact/JSON/header/date helpers `dnsid/jose.py:424`, `:458`, `:480`, `:491`; config/explicit expiry `:111`, `:149`; trusted audience `:218`; expiration recheck `:300`; OIDC reuse and recheck `dnsid/oidc.py:602`, `:666`, `:1003`; regression `tests/test_corrected_boundaries.py:21`. | Rejects duplicate JSON, unsupported critical/unencoded headers, mixed audiences, nonnumeric dates, and invalid explicit durations. Zero/fractional NumericDates are valid types; clock skew never extends expiry. |
| HTTP input bounds and current-peer propagation | [02: Resource limits](../02-rfc9421-http-message-signatures.md#resource-limits) | Implemented: `dnsid/http_signatures.py:406`, `:553`; regression `tests/test_corrected_boundaries.py:99`. | 64 KiB combined signature headers, 16 KiB digest header, 8 MiB buffered body, 16 labels, 64 components. Hosts must enforce body bounds while reading the already-buffered request. |

### HTTP Message Signatures

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Constrained component and Structured Field model | [02: Structured Field data model](../02-rfc9421-http-message-signatures.md#structured-field-data-model) | Implemented: ordered `SignatureParams` parsing/serialization `dnsid/models.py:1490`, `:1553`, `:1560`, `:1679`; component validation/base construction `dnsid/http_signatures.py:597`. | Unknown signature parameters are preserved; unsupported component parameters and duplicates fail closed. |
| Canonical signature-base construction | [02: Signature-base construction](../02-rfc9421-http-message-signatures.md#signature-base-construction), [shared vectors](../conformance/README.md#http-message-signatures) | Implemented: `dnsid/http_signatures.py:597`; shared HTTP-message-signature vectors pass. | Includes `@query-param`, dictionary-key selection, extension serialization, and duplicate rejection. |
| DNSid request signing | [02: DNSid request profile](../02-rfc9421-http-message-signatures.md#dnsid-request-profile) | Implemented: request/client helpers `dnsid/http_signatures.py:149`, `:233`, `:287`; content-presence handling `:257`, `:274`, `:317`, `:341`; manager transport inheritance `:88`, `:355`. | Exact profile defaults, nonce/algorithm policy, supplied empty-content semantics, and inherited transport settings match the design. |
| DNSid verification and Content-Digest | [02: Verification](../02-rfc9421-http-message-signatures.md#verification), [02: Content-Digest](../02-rfc9421-http-message-signatures.md#content-digest) | Implemented: candidate selection `dnsid/http_signatures.py:364`; candidate/digest verification `:459`, `:510`; current-peer propagation `:551`. | Accepts exactly one valid DNSid candidate and ignores unrelated invalid complete signatures. |

### JOSE JWT and JWS

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| JOSE standards/dependencies and profile configuration | [03: Standards](../03-jose-jwt-jws.md#standards) | Implemented: `JoseProfile` `dnsid/jose.py:64`; `JoseConfig` `:55`. | Manual compact JOSE uses SDK crypto and base64url helpers. |
| Application-layer algorithm policy | [03: Application-Layer Algorithm Policy](../03-jose-jwt-jws.md#application-layer-algorithm-policy) | Implemented: EdDSA/ES256 allow-list `dnsid/_utils.py:18`; enforced for JWT/JWS `dnsid/jose.py:282`, `:368`; both available from `LocalKeyProvider.generate` `dnsid/local_key_provider.py:527`. |  |
| `CreateJWT` / claims / reserved claim validation | [03: CreateJWT](../03-jose-jwt-jws.md#createjwtopts-jwtoptions---string) | Implemented: `dnsid/jose.py:128`; `JWTOptions` `dnsid/models.py:1438`; reserved claims `dnsid/jose.py:154`. |  |
| `VerifyJWT(jwt)` | [03: VerifyJWT](../03-jose-jwt-jws.md#verifyjwtjwt-string---verifieddomain) | Implemented: `dnsid/jose.py:189`; current-peer forwarding `:267`. | Depends on fail-closed `verify_domain`, inheriting configured acceptance; JOSE settings stay in `JoseConfig`. |
| `CreateJWS(payload)` / `VerifyJWS(jws)` | [03: CreateJWS](../03-jose-jwt-jws.md#createjwspayload-bytes---string) | Implemented: create `dnsid/jose.py:310`; verify `:344`; current-peer forwarding `:389`. |  |
| Application-layer key IDs and flows | [03: Application-Layer Key IDs](../03-jose-jwt-jws.md#application-layer-key-ids) | Implemented: `parse_key_id` `dnsid/_utils.py:169`; manager profile factory `dnsid/jose.py:73`. |  |

### Transport and Registry

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Transport responsibilities and secure defaults | [05: Transport Responsibilities](../05-transport-and-registry.md#transport-responsibilities) | Implemented: `TransportConfig` as `DnsidConfig.transport` `dnsid/models.py:143`; injected-consumer conflict rules `dnsid/manager.py:1938`; checked-IP/CA transport with exact/suffix host allowlisting `dnsid/safe_transport.py:63`, `:204`, `:79`; explicit environment loader defaults `dnsid/environment.py:119`, `:209`; streamed strict-URL core HTTPS helpers `dnsid/_https_client.py:33`, `:119`, `:261`; peer certificate read from httpx's `network_stream` `dnsid/_https_client.py:329`. | DNS and CA injection are implemented without global mutation; core response limits are streamed and private-service access is hostname-scoped. |
| `CreateDnsidHttpClient(transportConfig)` | [05: CreateDnsidHttpClient](../05-transport-and-registry.md#creatednsidhttpclienttransportconfig-transportconfig---httpclient) | Implemented: sync and async factories `dnsid/manager.py:1777`, `:1791`. |  |
| Registry client and default URL | [05: RegistryClient](../05-transport-and-registry.md#registryclient) | Implemented: base URL validation `dnsid/registry_client.py:84`, `:106`; default `dnsid/models.py:189`; abstract client/prepared-event methods `dnsid/interfaces.py:500`, `:547`; concrete client `dnsid/registry_client.py:51`. | HTTPS is required except exact literal loopback hosts for local testnets. |
| Publication authority model (client vs registry) | [05: Registry Responsibilities](../05-transport-and-registry.md#registry-responsibilities) | Implemented: typed authority/configuration `dnsid/models.py:1765`, `:1779`; fail-closed protocol and registration response parsing `dnsid/registry_client.py:1450`, `:1478`; client and registry gates `dnsid/manager.py:507`, `:542`, `:605`, `:645`. | Neither path substitutes the operational key for the entity key. |
| Managed Live registration and proof workflow | [05: LiveAgentRegistrationInput](../05-transport-and-registry.md#liveagentregistrationinput), [05: Live Provisioning Types](../05-transport-and-registry.md#live-provisioning-types) | Implemented: dedicated models are at `dnsid/models.py:1755`, `:1810`, `:1824`, `:1834`, `:1844`, `:1856`; exact-status authenticated operations are at `dnsid/registry_client.py:250`, `:449`, `:480`; public Ed25519 validation and transcript/key/domain binding are at `:1225`, `:1267`, `:1362`. | Reissue requires the original public key as non-wire context, validates it before network I/O, computes its RFC 7638 thumbprint, and binds the replacement transcript to it. |
| Client-controlled registry publication | [05: PublishClientControlledRecord](../05-transport-and-registry.md#publishclientcontrolledrecordregistryclient-registryclient---publishedrecord) | Implemented: `dnsid/manager.py:507`; authority/key gates `:542`, `:557`; parse/profile/tag comparison `:565`, `:573`, `:587`; signature submission `:595`; `ka` mismatch tests `tests/test_manager.py:437`, `:448`, `:461`. | All known tags are compared exactly and unknown signed tags are preserved. |
| Registry-managed publication wait | [05: AwaitRegistryManagedPublication](../05-transport-and-registry.md#awaitregistrymanagedpublicationregistryclient-registryclient---publishedrecord) | Implemented: `dnsid/manager.py:605`; authority/status checks `:645`, `:657`, `:663`; protocol-only DNS re-verification without acceptance `:666`, `:1308`; terminal registry status classification `dnsid/registry_client.py:47`. | Registry READY/DNS workflow state is not trusted as protocol verification; `REVOKED` and `RETIRED` are terminal. Public `verify_domain(local_domain)` still enforces acceptance (`tests/test_counterparty_acceptance.py:397`). |
| Registry endpoints and ordinary lifecycle helpers | [05: RegistryClient](../05-transport-and-registry.md#registryclient) | Implemented: exact HTTP 201 ordinary registration `dnsid/registry_client.py:167`, `:219`; verify/revoke/retire/cancel/challenge `:286`, `:305`, `:343`, `:357`, `:371`; status/poll `:506`, `:641`; canonical/publish `:783`, `:803`; prepare/submit `:822`, `:873`, `:954`; non-2xx responses raise typed `RegistryRequestError` without echoing bodies `:1095`, `dnsid/exceptions.py:158`. | Prepared events remain limited to issuance and rotation. Revocation and retirement are registry-owned mutation calls. |
| Managed operational key rotation flow (`ku` supersede) | [05: RegistryClient](../05-transport-and-registry.md#registryclient) | Implemented: `dnsid/manager.py:685`, `:833`, `:969`, `:1053`, `:1097`; registry prepare/submit `dnsid/registry_client.py:873`, `:954`; durable result `dnsid/models.py:1971`. | New-key activation occurs only after an accepted result binds to the exact persisted bytes and key. |
| `KeyRotationPreparationRequest` data type | [05: KeyRotationPreparationRequest](../05-transport-and-registry.md#keyrotationpreparationrequest) | Implemented: `dnsid/models.py:1927`; private-member rejection and kid/alg validation `dnsid/registry_client.py:904`, `:909`. |  |
| `SubmissionResult` typed result | [05: RegistryClient](../05-transport-and-registry.md#registryclient) | Implemented: `dnsid/models.py:1946`; response normalization and exact accepted hash `dnsid/registry_client.py:954`, `:1078`, `:1048`. | Pending/rejected results retain stable error codes; accepted responses must bind to submitted bytes. |
| Registry data types | [05: Registry Responsibilities](../05-transport-and-registry.md#registry-responsibilities) | Implemented: ordinary/Live registration and proof models `dnsid/models.py:1739`, `:1755`, `:1779`, `:1810`, `:1824`, `:1834`, `:1844`, `:1856`; publication/prepared-event models `:1919`, `:1927`, `:1946`; strict parsing `dnsid/registry_client.py:1267`, `:1478`. | Workflow, DNS publication, and protocol status remain separate; `AgentRegistration.oidc_issuer_url` preserves the exact registry issuer at `dnsid/models.py:1790`. |

### Log Bindings

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Log responsibilities and interfaces | [06: Responsibilities](../06-log-bindings.md#responsibilities) | Implemented: `Log` `dnsid/interfaces.py:178`; `LogReader`, exact-cutoff contract, and evidence-returning non-revocation contract `:211`, `:263`, `:236`; exact-cutoff result `dnsid/models.py:1090`; bilateral/continuity hooks `dnsid/interfaces.py:292`, `:314`; C2SP implementation `dnsid/c2sp_tlog/reader.py:719`. | Lifecycle read/write responsibilities, method-neutral bounded predecessor verification, and structured operation-level evidence are implemented. |
| `DomainLog`, `DomainSnapshot`, shared lifecycle reducer | [06: Implemented lifecycle interpretation](../06-log-bindings.md#implemented-lifecycle-interpretation), [06: DomainLog](../06-log-bindings.md#domainlog) | Implemented: `DomainSnapshot`/`DomainLog` `dnsid/models.py:1232`, `:1249`; reducer `:1259`; C2SP terminal-history checks `dnsid/c2sp_tlog/reader.py:473`, `:538`, `:564`. | Verified history remains separate from current protocol status. |
| `LogRegistry` | [06: LogRegistry](../06-log-bindings.md#logregistry) | Implemented: `dnsid/registry.py:16`. |  |
| Production log reader / no-op behavior | [06: LogReader Interface](../06-log-bindings.md#logreader-interface) | Implemented: fail-closed `NoopLogReader` `dnsid/interfaces.py:342`; C2SP reader `dnsid/c2sp_tlog/reader.py:248`; draft 01 capability enforcement `dnsid/manager.py:2209`. |  |
| Required and optional log events | [06: Log Events](../06-log-bindings.md#log-events) | Implemented: event models `dnsid/models.py:960`, `:983`; C2SP mapping `dnsid/c2sp_tlog/event_codec.py:67`; signer roles `dnsid/c2sp_tlog/prepared.py:37`; lifecycle verification `dnsid/c2sp_tlog/stream_verifier.py:404`. | All six version-1 lifecycle event types and role signatures are implemented. |
| Method-owned metadata and verified lifecycle ordering | [06: Method-owned metadata](../06-log-bindings.md#method-owned-metadata) | Implemented: generic lifecycle-only events `dnsid/models.py:960`; C2SP context/chain metadata `dnsid/c2sp_tlog/event_codec.py:47`; index-order selection `dnsid/c2sp_tlog/stream_verifier.py:87`; prefix/state-machine enforcement `dnsid/models.py:1259`. | C2SP owns and verifies method metadata before returning ordinary lifecycle events. |

### C2SP Transparency Log Binding

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| C2SP binding responsibilities and package surface | [11: Responsibilities](../11-c2sp-tlog-binding.md#responsibilities) | Implemented: exports `dnsid/c2sp_tlog/__init__.py:191`; reader/binding `dnsid/c2sp_tlog/reader.py:248`, `:1382`. |  |
| Exact C2SP specification version identification | [11: Standards and Versioning](../11-c2sp-tlog-binding.md#standards-and-versioning) | Implemented: complete pinned `C2SP_SPEC_REVISIONS` `dnsid/c2sp_tlog/__init__.py:175`; envelope version `:188`; exact-map test `tests/test_public_api.py:63`. | The corrected DNSid method revision is additionally pinned in `dnsid/conformance.py:32`. |
| C2SP trust profiles and signer rotation | [11: Trust Profiles](../11-c2sp-tlog-binding.md#trust-profiles) | Implemented: exact-member parser `dnsid/c2sp_tlog/trust_profile.py:34`; scope/prefix, policy-origin, key-ID, key-distinctness, and role-separation validation `:76`, `:94`, `:98`; public exports `dnsid/c2sp_tlog/__init__.py:154`, `:191`. | Caller-supplied version-1 profiles constrain readers to one exact scope and canonical log prefix and support overlapping verifier keys. |
| `c2sp-tlog` reference parsing, identity-instance generation, canonicalization, origin, and final index | [11: Reference and Binding Context](../11-c2sp-tlog-binding.md#reference-and-binding-context) | Implemented: stream ID generation/parser/origin `dnsid/c2sp_tlog/lr.py:42`, `:51`, `:159`; logical event ID and signed bytes `dnsid/c2sp_tlog/event_codec.py:247`, `:259`; reader identity-instance rejection `:391`; writer rejection `dnsid/c2sp_tlog/prepared.py:312`. | Readers validate the signed FQDN against the stream ID; accepting an opaque reference syntactically does not bypass that check. The former bare-FQDN reader deviation is resolved. |
| Version-1 event envelope, JCS, supported event types, and size limits | [11: Event Envelope](../11-c2sp-tlog-binding.md#event-envelope) | Implemented: codec `dnsid/c2sp_tlog/event_codec.py:67`; parser `:130`; JCS `dnsid/c2sp_tlog/canonical.py:19`; size limit `dnsid/c2sp_tlog/event_codec.py:470`. | Signed bytes exclude only top-level signatures; unknown signed fields remain covered. |
| Corrected logical event identity and all-scope chaining | [11: Signed Context and Stream Chain](../11-c2sp-tlog-binding.md#signed-context-and-stream-chain), [11: Stream Completeness and Logged State](../11-c2sp-tlog-binding.md#stream-completeness-and-logged-state) | Implemented: signature-independent bytes and zero-byte-separated event IDs `dnsid/c2sp_tlog/event_codec.py:247`, `:259`; historical-authority selection, logical replay, and occurrence verification `dnsid/c2sp_tlog/stream_verifier.py:87`, `:222`; chain/state validation `dnsid/c2sp_tlog/stream_verifier.py:629`; focused regression `tests/test_c2sp_logical_identity.py:26`. | Corrected logical IDs deduplicate signature variants while distinct signed payloads and authenticated forks remain fatal; exact occurrence evidence is retained. |
| Lifecycle signer roles and signature verification | [11: Lifecycle Signatures](../11-c2sp-tlog-binding.md#lifecycle-signatures) | Implemented: roles `dnsid/c2sp_tlog/prepared.py:37`; signature requirements `dnsid/c2sp_tlog/event_codec.py:326`; lifecycle verification `dnsid/c2sp_tlog/stream_verifier.py:404`. | Includes binding-owned `NewOperational` proof. |
| Prepared event parse/sign/serialize/write surface | [11: Prepared Event Model](../11-c2sp-tlog-binding.md#prepared-event-model) | Partial: model/prepare `dnsid/c2sp_tlog/prepared.py:96`, `:119`; untrusted parse `:145`; role signing `:174`; complete bytes `:242`; safe write `:278`. | Unknown signed envelope fields remain covered and preserved. Prepared events currently lack the corrected `eventId` property (tracked in Findings). |
| Submission adapter, durable idempotency, managed rotation, and stale-preparation behavior | [11: Submission and Idempotency](../11-c2sp-tlog-binding.md#submission-and-idempotency) | Implemented for the SDK contract: raw transport type `dnsid/models.py:1919`; abstract prepare/submit methods `dnsid/interfaces.py:547`, `:556`, `:569`; registry prepare/submit `dnsid/registry_client.py:822`, `:873`, `:954`; durable manager recovery `dnsid/manager.py:685`, `:833`, `:969`, `:1053`. | Registry preparation remains untrusted until binding parsing; exact bytes and idempotency keys survive retries. |
| Portable checkpoint/tile/entry read surface and source abstraction | [11: Read Surface](../11-c2sp-tlog-binding.md#read-surface) | Implemented: source/evidence interfaces `dnsid/c2sp_tlog/stream_source.py:64`, `:74`, `:89`; bounded scanner `:117`; consistency source `:107`; tile bundle parser `dnsid/c2sp_tlog/tiles.py:48`; portable bundle verifier `dnsid/c2sp_tlog/stream_bundle.py:91`, `:114`, `:265`; `ReadEvent` discovery and bundle-backed authorization `dnsid/c2sp_tlog/reader.py:587`, `:659`. | Standard entry-bundle geometry is fixed at 256. With bundle trust configured, `ReadEvent` verifies the exact requested occurrence under historical authority rather than substituting an earlier copy. |
| Global-log candidate selection and authenticated contradictions | [11: Read Surface](../11-c2sp-tlog-binding.md#read-surface), [11: Stream Completeness and Logged State](../11-c2sp-tlog-binding.md#stream-completeness-and-logged-state) | Implemented: selection/deduplication/authenticated failure `dnsid/c2sp_tlog/stream_verifier.py:87`, `:160`, `:185`, `:283`; stable categories `dnsid/c2sp_tlog/errors.py:12`; tests `tests/test_c2sp_tlog.py:521`, `:1111`, `:1134`. | Unauthenticated noise is ignored; authenticated chain, transition, and post-terminal contradictions are fatal. |
| Checkpoint, inclusion proof, append-only consistency, policy, and witness verification | [11: Checkpoint, Proof, and Policy Verification](../11-c2sp-tlog-binding.md#checkpoint-proof-and-policy-verification) | Implemented: proof `dnsid/c2sp_tlog/proof.py:66`; policy/quorum `dnsid/c2sp_tlog/policy.py:131`, `:178`; atomic checkpoint store `dnsid/c2sp_tlog/checkpoint_store.py:58`; durable `SQLiteCheckpointStore` `dnsid/c2sp_tlog/sqlite_checkpoint_store.py:20`; scan consistency `dnsid/c2sp_tlog/reader.py:893`, `:1136`; rollback/fork/inconsistency reported as typed `C2spCheckpointConsistencyError` with trusted and observed checkpoints `dnsid/c2sp_tlog/errors.py:63`; RFC 6962 consistency `dnsid/c2sp_tlog/merkle.py:52`; bundle consistency `dnsid/c2sp_tlog/stream_bundle.py:494`, `:536`. | Process-lifetime and injectable durable checkpoint stores are supported; verification never resets trust and re-baselining requires an explicit audited operation (`tests/test_sqlite_checkpoint_store.py:60`, `:70`). |
| Complete logged state, non-revocation, lifecycle ordering, and migration stitching | [11: Destination-Only Migration](../11-c2sp-tlog-binding.md#destination-only-migration), [11: Stream Completeness and Logged State](../11-c2sp-tlog-binding.md#stream-completeness-and-logged-state) | Implemented: evidence-returning freshness/non-revocation `dnsid/c2sp_tlog/reader.py:546`, `:575`; C2SP exact-cutoff implementation `:719`; recursive method dispatch and shared lifecycle/key checks `:925`, `:1006`, `:1030`; exact cutoff/resource/cycle validation `:1060`; tests `tests/test_c2sp_migration.py:546`, `:587`, `:608`, `:626`, `:649`. | Registered non-C2SP readers can return exact-cutoff verified history through `LogReader.rebuild_history_through`; C2SP retains ACTIVE/entity/operational continuity, cycle detection, and cumulative depth/event/response limits before stitching. |
| Standalone stream-bundle migration verification | [11: Destination-Only Migration](../11-c2sp-tlog-binding.md#destination-only-migration) | Partial: `dnsid/c2sp_tlog/stream_bundle.py:265`, `:398`; `tests/test_c2sp_migration.py:452`, `:436`. | Without a predecessor resolver the standalone API rejects migration genesis. Configured readers provide recursive verification (`tests/test_c2sp_migration.py:388`, `:436`). The design does not require a second standalone resolver API; this limitation does not downgrade those reader paths. |
| Scope-specific transport and trust policy | [11: Scope and Trust Policy](../11-c2sp-tlog-binding.md#scope-and-trust-policy) | Implemented: reference scope/HTTPS validation `dnsid/c2sp_tlog/lr.py:51`; policy rules `dnsid/c2sp_tlog/policy.py:69`, `:131`; explicit fetch guarantees/public enforcement `dnsid/c2sp_tlog/resource_fetcher.py:20`, `dnsid/c2sp_tlog/stream_source.py:271`; reader boundary `dnsid/c2sp_tlog/reader.py:392`. | Public sources must attest all required transport capabilities. |
| Verification convenience factory | [11: Verification Convenience Factory](../11-c2sp-tlog-binding.md#verification-convenience-factory) | Implemented: options/factory and migration limits `dnsid/c2sp_tlog/verification_registry.py:40`, `:89`, `:138`, `:187`; registry-wide predecessor dispatch `:221`; preferred-bundle availability fallback `dnsid/c2sp_tlog/stream_bundle.py:192`; exact bundle binding `:211`, `:311`; missing-consistency fallback `:226`; regression `tests/test_c2sp_migration.py:525`. | A completely read signed bundle with the wrong `lr` fails closed. Only listed availability failures and otherwise-valid bundles missing consistency evidence can use raw scanning; required-bundle mode disables both. |
| Identity Digital-managed trust factory | [11: Identity Digital-Managed Trust](../11-c2sp-tlog-binding.md#identity-digital-managed-trust) | Implemented: reviewed development and production trust profiles and exact catalog selectors are at `dnsid/c2sp_tlog/managed_verification_registry.py:23`, `:33`, `:66`; opt-in construction, profile parsing, generic-registry reuse, shared infrastructure, fixed defaults, and exact dispatch are at `:80`, `:115`, `:123`, `:152`, `:157`. | Both catalog entries prefer verified stream bundles with the specified bounded raw-scan fallback. |
| C2SP error taxonomy | [11: Errors](../11-c2sp-tlog-binding.md#errors) | Implemented: categories/errors `dnsid/c2sp_tlog/errors.py:12`, `:39`, `:107`; HTTP/transport classification `dnsid/c2sp_tlog/resource_fetcher.py:90`; shared boundary adaptation `dnsid/c2sp_tlog/reader.py:224`, `:1438`; adversarial tests `tests/test_c2sp_tlog.py:1884`, `:1911`. | Unsafe transport, parse, policy, integrity, and limit failures are permanent; only specified availability failures are transient/fallback-eligible. |
| C2SP conformance and cross-SDK interoperability tests | [11: Conformance and Interoperability](../11-c2sp-tlog-binding.md#conformance-and-interoperability), [shared vectors](../conformance/README.md) | Implemented: production wire, split-signing, lifecycle-selection, trust-profile, managed-selection, recursive cross-method migration, standard-resource factory, preferred-bundle, portable-bundle, and central matrix tests pass at reviewed SDK commit `1257fd0` and design commit `1d3fbb5`. | `.venv/bin/pytest -q` passed 1285 tests; direct shared harness and matrix commands also passed. The matrix does not establish full recursive migration interoperability. |

### Protocol Data Types

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Identity record behavior profiles and dispatch | [07: Identity Record Behavior Profiles](../07-protocol-data-types.md#identity-record-behavior-profiles) | Implemented: selectors/default `dnsid/models.py:589`, `:602`; required tags `:609`; unsupported rejection `:684`; exact-selector canonicalization `:870`; verification dispatch `dnsid/manager.py:1523`. | Publishes only `dnsid-draft-01`; verifies it and exact pre-RFC `DNSid1`. |
| Parse / serialize / canonical / validate behavior | [07: DnsIdTxtRecord](../07-protocol-data-types.md#dnsidtxtrecord) | Implemented: parse `dnsid/models.py:668`; unsigned parse `:734`; validate `:808`; canonical `:870`; serialize `:891`. | Unknown syntactically valid tags are preserved and signed; `gi`, `ek`, and `ku` profile constraints are enforced. |
| FQDN normalization and `gi` helpers | [07: DnsIdTxtRecord](../07-protocol-data-types.md#dnsidtxtrecord) | Implemented: `normalize_fqdn` `dnsid/_utils.py:103`; `is_domain_name` `:154`; delegated `gi` is accepted structurally and bound through ISSUANCE `dnsid/c2sp_tlog/reader.py:451`. |  |
| `JWKS` and key constraints | [07: JWKS](../07-protocol-data-types.md#jwks) | Implemented: public serialization `dnsid/models.py:224`, `:297`; `JWKS` `:292`; `signature_alg` `:255`; current role keys `:334`, `:342`; role validation `:351`, `:355`; generic validation `:394`; distinctness helper `:457`. | Exactly one current signing key per draft 01 role with explicit consistent `alg` and `kid`; serializers return independent public-only dictionaries. |
| `VerifiedDomain` data type | [07: VerifiedDomain](../07-protocol-data-types.md#verifieddomain) | Implemented: core data/evidence `dnsid/models.py:1127`; cache-state, policy accessor, and expiry `:1176`, `:1184`, `:1188`; construction `dnsid/manager.py:1616`. | Runtime and record-signing evidence are retained, expiry includes both TLS bounds, and `requires_log_check()` exposes the operation-level policy signal without I/O. No trust flag or acceptance decision is stored (`tests/test_counterparty_acceptance.py:307`). |
| Wire version value and compatibility policy | [07: Version String Convention](../07-protocol-data-types.md#version-string-convention) | Implemented: `IDENTITY_RECORD_VERSION` and allow-lists `dnsid/models.py:594`, `:601`, `:602`; unsupported rejection `:684`. | Exact parsed selectors are never rewritten before signature verification. |
| SDK conformance metadata | [README: Conformance Metadata](../README.md#conformance-metadata) | Partial: immutable `SDK_CONFORMANCE` is at `dnsid/conformance.py:11`, `:37` and exported at `dnsid/__init__.py:89`; `known_deviations` is empty at `dnsid/conformance.py:47`. | The corrected method revision is pinned. Empty deviations no longer conflict with bare-FQDN reader behavior, but the prepared-event metadata and OIDC response-bound gaps below are not declared. |
| Owner-key selection / `sg` semantics | [07: Tags](../07-protocol-data-types.md#tags) | Implemented: `ek` selection/cardinality and canonical bytes `dnsid/manager.py:1521`, `:1528`; bare decode/verify `:1532`, `:1538`, `:1544`. | Detached JWS and algorithm-prefixed forms are rejected. |
| DNSid key service JWKS records | [07: DNSid Key Service JWKS Records](../07-protocol-data-types.md#dnsid-key-service-jwks-records) | Implemented: publication `dnsid/manager.py:421`, `:435`; verification `:1528`, `:1554`; role selectors `dnsid/models.py:334`, `:342`; public JSON serialization `:224`, `:297`. |  |
| TLS certificate representation and peer matching | [07: TLS Certificate](../07-protocol-data-types.md#tls-certificate) | Implemented: `TLSCertificate` and SAN-only matcher `dnsid/models.py:492`, `:504`; extraction `dnsid/_https_client.py:329`. | Wildcards match exactly one leftmost label; CN fallback is not used. |

### OIDC Federation

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| OIDC profile dependencies/configuration | [09: Dependencies](../09-oidc-federation.md#dependencies) | Implemented: `OIDCProfile` `dnsid/oidc.py:199`; `OIDCConfig` `:58`. |  |
| `OIDCProfile.from_identity_manager` | [09: Public Surface](../09-oidc-federation.md#public-surface) | Implemented: `dnsid/oidc.py:210`. |  |
| `CreateOIDCAssertion(opts)` | [09: CreateOIDCAssertion](../09-oidc-federation.md#createoidcassertionopts-oidcassertionoptions---string) | Implemented: `dnsid/oidc.py:271`; reserved claims including `fqdn` `:296`. |  |
| `DiscoverOIDCIssuer(issuer)` and issuer validation | [09: DiscoverOIDCIssuer](../09-oidc-federation.md#discoveroidcissuerissuer-string---oidcdiscoverydocument) | Implemented: discovery `dnsid/oidc.py:316`; exact issuer `:776`; same-origin endpoints `:820`; no redirects/body cap `:855`, `:940`. |  |
| `ExchangeOIDCToken` / `GetOIDCToken` | [09: ExchangeOIDCToken](../09-oidc-federation.md#exchangeoidctokenopts-oidctokenexchangeoptions---oidctokenresponse) | Partial: exchange `dnsid/oidc.py:383`; mint/get `:463`, `:478`; token-response cap/check `:886`, `:938`. | The token response is size-checked after buffered POST completion rather than bounded during reads; see Findings. |
| `VerifyOIDCToken(token, opts)` | [09: VerifyOIDCToken](../09-oidc-federation.md#verifyoidctokentoken-string-opts-verifyoidctokenoptions---verifiedoidcsubject) | Implemented: `dnsid/oidc.py:563`; issuer/audience/algorithm gates `:592`, `:598`, `:612`; current-peer DNSid subject verification `:660`; time checks `:1010`. | Subject verification inherits acceptance through the resolver; explicit `verify_dnsid_subject=False` (`dnsid/oidc.py:172`) yields no verified domain and is not a fallback. |
| OIDC response/data/error types | [09: Public Surface](../09-oidc-federation.md#public-surface) | Implemented: response/subject `dnsid/oidc.py:139`, `:176`; `NetworkError`/`OAuthError` `:39`, `:43`. |  |
| Optional `OIDCProvider` server surface | [09: Optional Provider Surface](../09-oidc-federation.md#optional-provider-surface) | Not applicable | Optional per design; no provider is implemented. |

### Web Bot Auth

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| WBA request signing | [10: Request signing](../10-web-bot-auth.md#request-signing) | Implemented: configuration bounds `dnsid/wba_signer.py:51`, `:71`; signing/content/component policy `:172`, `:211`, `:234`, `:243`. | Ed25519, thumbprint, nonce, tag, supplied empty content, and bounded lifetime match the profile. |
| `Signature-Agent` discovery | [10: Signature-Agent discovery](../10-web-bot-auth.md#signature-agent-discovery) | Implemented: discovery validation `dnsid/wba_signer.py:71`, `:149`; member-preserving update `:220`, `:286`. | Emits explicit `directory` or `jwks_uri` semantics. |
| Directory response | [10: Directory serving](../10-web-bot-auth.md#directory-serving) | Implemented: directory key conversion/serving `dnsid/wba_signer.py:314`, `:343`. | Covers required response components with bounded lifetime and canonical RFC 9421 metadata. |
| WBA verification | [10: Request verification requirements](../10-web-bot-auth.md#request-verification-requirements), [10: Directory verification requirements](../10-web-bot-auth.md#directory-verification-requirements) | Not applicable | Future public verifier API; the current signer/directory cut does not claim it. |

### Open Questions

| Design Item | Design Link | Evidence | Notes |
| --- | --- | --- | --- |
| Track implementation choices affected by resolved design questions | [08: SDK Design Open Questions](../08-open-questions.md) | Not applicable: there are no unresolved shared questions. Implemented resolved choices include lifecycle reduction `dnsid/models.py:1259`, operation-level evidence `dnsid/manager.py:1662`, managed rotation `:685`, and C2SP selection/stitching `dnsid/c2sp_tlog/stream_verifier.py:87`, `dnsid/c2sp_tlog/reader.py:1330`. |  |

## Findings

| Design Item | Implementation Reference | Finding |
| --- | --- | --- |
| Standalone stream-bundle migration genesis | `dnsid/c2sp_tlog/stream_bundle.py:265`, `:398`; `tests/test_c2sp_migration.py:452`, `:436`; `dnsid/conformance.py:47` | **API limitation, not missing reader migration.** The standalone verifier has no predecessor resolver and fails closed. Configured readers verify predecessor histories recursively. No new standalone API is required solely to clear this row. |
| Bare-FQDN reference readers | `dnsid/c2sp_tlog/event_codec.py:391`; `dnsid/c2sp_tlog/prepared.py:312` | **Resolved.** Both reader signed-context validation and writers reject the identity FQDN as the stream ID. |
| Prepared C2SP event metadata | `dnsid/c2sp_tlog/prepared.py:96`, `:327` | **Partial.** Prepared events derive signed bytes locally but expose no `eventId` field/property required by the corrected prepared-event model. Add it before claiming complete prepared-event surface. |
| OIDC token-endpoint response limit | `dnsid/oidc.py:886`, `:938` | **Partial.** The token POST uses buffered `httpx.Client.post`; the maximum response size is checked only after buffering, not while reading. Use a bounded streamed POST before relying on this limit against untrusted issuers. |
| SDK conformance metadata | `dnsid/conformance.py:47` | **Partial.** `known_deviations` is empty despite the prepared-event metadata and OIDC response-bound gaps above. The old bare-FQDN reader finding is resolved. |
| Consolidated configuration and counterparty acceptance | `dnsid/manager.py:130`, `:1312`, `:1329`, `:1887`, `:1938` | **Mostly resolved; one Partial row.** The `DnsidConfig`/`trusted_entities` contract is implemented and tested; former `Pre-redesign`/`TBD` rows are superseded. 0.19.1 resolved the unknown-field error type (`ArgumentError`) and retains the entity-key thumbprint on `VerifiedDomain`. Still open: constructor-time type validation of `identity` URL/profile fields and `transport` string fields is absent; the design requires it for dynamically typed configuration. |
| Validation scope | `tests/` | `.venv/bin/pytest -q` (1285 tests), the 193-expectation shared harness, and the C2SP matrix all passed at the reviewed head; the constructor redesign is cross-cutting, so the shared suites were rerun rather than carried forward. |
| Initial issuance recovery | `dnsid/manager.py:421`, `:435`, `:453`; `dnsid/c2sp_tlog/prepared.py:119` | **Evidence incomplete.** Publication/signing primitives and registry transport do not demonstrate a durable end-to-end issuance/recovery composition. Identify and test its coordinator before marking the whole flow Implemented. |
